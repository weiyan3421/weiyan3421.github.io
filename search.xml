<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>6.数据分组与聚合</title>
      <link href="/2025/6-%E6%95%B0%E6%8D%AE%E5%88%86%E7%BB%84%E4%B8%8E%E8%81%9A%E5%90%88/"/>
      <url>/2025/6-%E6%95%B0%E6%8D%AE%E5%88%86%E7%BB%84%E4%B8%8E%E8%81%9A%E5%90%88/</url>
      
        <content type="html"><![CDATA[<h3 id="初始化与加载数据"><a href="#初始化与加载数据" class="headerlink" title="初始化与加载数据"></a><strong>初始化与加载数据</strong></h3><p>首先，我们导入 <code>pandas</code> 库，并加载本次练习所需的数据集 <code>某招聘网站数据.csv</code>。题目要求对 <code>createTime</code> 列进行操作，所以在加载时使用 <code>parse_dates</code> 参数直接将其转换为日期时间类型，这是一个非常好的习惯。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> pandas <span class="hljs-keyword">as</span> pd<br><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np <span class="hljs-comment"># 导入numpy，后面聚合操作可能会用到</span><br><br><span class="hljs-comment"># 加载数据，并直接将 createTime 列解析为日期时间类型</span><br>df = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, parse_dates=[<span class="hljs-string">&#x27;createTime&#x27;</span>])<br></code></pre></td></tr></table></figure><hr><h2 id="分组"><a href="#分组" class="headerlink" title="分组"></a><strong>分组</strong></h2><p><code>groupby</code> 是 Pandas 数据分析的基石，它遵循“分割-应用-合并”（Split-Apply-Combine）的原则来对数据进行处理和分析。</p><h3 id="1-分组统计｜均值"><a href="#1-分组统计｜均值" class="headerlink" title="1. 分组统计｜均值"></a><strong>1. 分组统计｜均值</strong></h3><ul><li><strong>题目</strong>: 计算各区(<code>district</code>)的薪资(<code>salary</code>)均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">mean_salary_by_district = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].mean()<br><span class="hljs-built_in">print</span>(mean_salary_by_district)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ol><li><strong><code>df.groupby(&#39;district&#39;)</code></strong>: 这是“分割”步骤。Pandas 会根据 <code>district</code> 列中的唯一值（如’上城区’, ‘下沙’等）将整个 DataFrame 分割成多个子集（组）。</li><li><strong><code>[&#39;salary&#39;]</code></strong>: 在分割后的每个组上，我们指定要操作的列是 <code>salary</code>。</li><li><strong><code>.mean()</code></strong>: 这是“应用-合并”步骤。对每个组的 <code>salary</code> 列计算其均值（mean），然后将所有组的结果合并成一个新的 Series，其中索引就是分组的依据——各个区名。</li></ol></li></ul><h3 id="2-分组统计｜取消索引"><a href="#2-分组统计｜取消索引" class="headerlink" title="2. 分组统计｜取消索引"></a><strong>2. 分组统计｜取消索引</strong></h3><ul><li><strong>题目</strong>: 重新按照上一题要求进行分组，但不使用 <code>district</code> 做为索引。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">mean_salary_no_index = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>, as_index=<span class="hljs-literal">False</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].mean()<br><span class="hljs-built_in">print</span>(mean_salary_no_index)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>groupby</code> 函数有一个非常有用的参数 <code>as_index</code>。</li><li>默认情况下 <code>as_index=True</code>，会将分组的键（这里是 <code>district</code>）作为结果的索引。</li><li>当我们设置为 <strong><code>as_index=False</code></strong> 时，分组的键会保留为一个普通的列，而结果会使用一个从 0 开始的默认整数索引。这在后续需要将结果作为普通表格处理时非常方便。</li></ul></li></ul><h3 id="3-分组统计｜排序"><a href="#3-分组统计｜排序" class="headerlink" title="3. 分组统计｜排序"></a><strong>3. 分组统计｜排序</strong></h3><ul><li><strong>题目</strong>: 计算并提取平均薪资最高的区。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">highest_salary_district = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].mean().sort_values(by=<span class="hljs-string">&#x27;salary&#x27;</span>, ascending=<span class="hljs-literal">False</span>).head(<span class="hljs-number">1</span>)<br><span class="hljs-built_in">print</span>(highest_salary_district)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>这是一个链式操作，将多个步骤连接在一起：</li></ul><ol><li><code>df.groupby(&#39;district&#39;)[&#39;salary&#39;].mean()</code>: 首先，像第一题一样计算出各区的平均薪资。</li><li><strong><code>.sort_values(ascending=False)</code></strong>: 接着，对上一步得到的结果（一个以区名为索引的 Series）进行排序。<code>ascending=False</code> 表示<strong>降序</strong>排列，即薪资最高的区会排在最前面。</li><li><strong><code>.head(1)</code></strong>: 最后，使用 <code>.head(1)</code> 提取排序后的第一行，即为平均薪资最高的那个区。</li></ol></li></ul><h3 id="4-分组统计｜频率"><a href="#4-分组统计｜频率" class="headerlink" title="4. 分组统计｜频率"></a><strong>4. 分组统计｜频率</strong></h3><ul><li><strong>题目</strong>: 计算不同行政区(<code>district</code>)，不同规模公司(<code>companySize</code>)出现的次数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">freq_by_dist_size = df.groupby([<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;companySize&#x27;</span>]).size()<br><span class="hljs-built_in">print</span>(freq_by_dist_size)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ol><li><strong><code>df.groupby([&#39;district&#39;, &#39;companySize&#39;])</code></strong>: 这里我们按<strong>多个列</strong>进行分组。只需将列名列表传给 <code>groupby</code> 即可。Pandas 会根据 <code>district</code> 和 <code>companySize</code> 的<strong>组合</strong>来创建分组。</li><li><strong><code>.size()</code></strong>: 这是一个专门用于 <code>groupby</code> 对象的聚合函数，它会直接返回每个分组包含的<strong>行数（即频率）</strong>。这比 <code>.count()</code> 更直接，因为它不关心任何特定列的内容。</li><li>结果的索引是一个**多级索引 (MultiIndex)**，由 <code>district</code> 和 <code>companySize</code> 两个层级构成。</li></ol></li></ul><h3 id="5-分组统计｜修改索引名"><a href="#5-分组统计｜修改索引名" class="headerlink" title="5. 分组统计｜修改索引名"></a><strong>5. 分组统计｜修改索引名</strong></h3><ul><li><strong>题目</strong>: 将上一题的索引名修改为 <code>行政区</code> 和 <code>公司规模</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先得到上一题的结果</span><br>freq_by_dist_size = df.groupby([<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;companySize&#x27;</span>]).size()<br><br><span class="hljs-comment"># 修改索引名</span><br>freq_by_dist_size.index.names = [<span class="hljs-string">&#x27;行政区&#x27;</span>, <span class="hljs-string">&#x27;公司规模&#x27;</span>]<br><span class="hljs-built_in">print</span>(freq_by_dist_size)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>对于一个带有索引的 Series 或 DataFrame，可以通过访问其 <code>.index.names</code> 属性来修改索引的名称。</li><li>因为上一题的结果是多级索引，<code>.index.names</code> 是一个列表。我们直接将一个新的名称列表赋给它即可。</li></ul></li></ul><h3 id="6-分组统计｜计数"><a href="#6-分组统计｜计数" class="headerlink" title="6. 分组统计｜计数"></a><strong>6. 分组统计｜计数</strong></h3><ul><li><strong>题目</strong>: 计算上一题，每个区出现的公司数量。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先得到第4题的结果</span><br>freq_by_dist_size = df.groupby([<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;companySize&#x27;</span>]).size()<br><span class="hljs-comment"># 在多级索引上按第一层级(&#x27;district&#x27;)求和</span><br>count_by_district = freq_by_dist_size.groupby(level = <span class="hljs-string">&#x27;district&#x27;</span>).<span class="hljs-built_in">sum</span>()<br><span class="hljs-built_in">print</span>(count_by_district)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>这个问题可以理解为：在第4题按“区-公司规模”分组计数的基础上，再按“区”进行一次聚合（求和）。</li><li>对于一个多级索引的 Series，调用 <code>.sum()</code>、<code>.mean()</code> 等聚合函数时，可以通过 <code>level</code> 参数指定在哪一个索引层级上进行运算。这里 <code>level=&#39;district&#39;</code> 告诉 Pandas 沿着 <code>district</code> 这一层索引来求和。</li></ul></li></ul><h3 id="7-分组查看｜全部"><a href="#7-分组查看｜全部" class="headerlink" title="7. 分组查看｜全部"></a><strong>7. 分组查看｜全部</strong></h3><ul><li><strong>题目</strong>: 将数据按照 <code>district</code>、<code>salary</code> 进行分组，并查看各分组内容。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">grouped_obj = df.groupby([<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>])<br><span class="hljs-built_in">print</span>(grouped_obj.groups)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>当我们执行 <code>df.groupby(...)</code> 时，会创建一个 <code>DataFrameGroupBy</code> 对象。这个对象本身并不直接显示数据。</li><li>想要查看分组情况，可以访问其 <code>.groups</code> 属性。</li><li><code>.groups</code> 会返回一个字典，其中：<ul><li><strong>键 (key)</strong> 是一个元组，代表分组的依据，例如 <code>(&#39;上城区&#39;, 22500)</code>。</li><li><strong>值 (value)</strong> 是一个列表，包含了属于该分组的所有行的<strong>原始索引</strong>。</li></ul></li></ul></li></ul><h3 id="8-分组查看｜指定"><a href="#8-分组查看｜指定" class="headerlink" title="8. 分组查看｜指定"></a><strong>8. 分组查看｜指定</strong></h3><ul><li><strong>题目</strong>: 将数据按照 <code>district</code>、<code>salary</code> 进行分组，并查看西湖区薪资为 30000 的工作。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">grouped_obj = df.groupby([<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>])<br>xihu_30k_jobs = grouped_obj.get_group((<span class="hljs-string">&#x27;西湖区&#x27;</span>, <span class="hljs-number">30000</span>))<br><span class="hljs-built_in">print</span>(xihu_30k_jobs)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>.get_group()</code> 是从 <code>GroupBy</code> 对象中提取<strong>特定单个分组</strong>数据的方法。</li><li>因为我们是按 <code>[&#39;district&#39;, &#39;salary&#39;]</code> 两个键分组的，所以需要向 <code>.get_group()</code> 传入一个<strong>元组 <code>(&#39;西湖区&#39;, 30000)</code></strong> 来指定想要查看的组。</li><li>该方法会返回一个包含该组所有原始数据的完整 DataFrame。</li></ul></li></ul><h3 id="9-分组规则｜通过匿名函数1"><a href="#9-分组规则｜通过匿名函数1" class="headerlink" title="9. 分组规则｜通过匿名函数1"></a><strong>9. 分组规则｜通过匿名函数1</strong></h3><ul><li><strong>题目</strong>: 根据 <code>createTime</code> 列，计算每天不同行政区新增的岗位数量。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">daily_district_counts = df.groupby([df[<span class="hljs-string">&#x27;createTime&#x27;</span>].dt.day, <span class="hljs-string">&#x27;district&#x27;</span>]).size().rename_axis([<span class="hljs-string">&#x27;发布日&#x27;</span>, <span class="hljs-string">&#x27;行政区&#x27;</span>])<br><span class="hljs-built_in">print</span>(daily_district_counts)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>groupby</code> 的分组依据非常灵活，不一定非得是列名。它可以是一个与原 DataFrame 行数相同的<strong>数组或 Series</strong>。</li></ul><ol><li><strong><code>df[&#39;createTime&#39;].dt.day</code></strong>: <code>.dt</code> 是处理日期时间类型列的访问器 (accessor)。<code>.dt.day</code> 会提取出 <code>createTime</code> 列中每一天的“日”部分（如16, 15, 14等），形成一个新的 Series。</li><li><strong><code>df.groupby([..., &#39;district&#39;])</code></strong>: 我们将这个新生成的“日”Series和 <code>district</code> 列名一起放到一个列表中传给 <code>groupby</code>，实现按“日-区”的分组。</li><li><strong><code>.size()</code></strong>: 统计每个“日-区”组合的岗位数。</li><li><strong><code>.rename_axis(...)</code></strong>: 为了让结果更清晰，使用 <code>.rename_axis()</code> 给两个索引层级命名。</li></ol></li></ul><h3 id="10-分组规则｜通过匿名函数2"><a href="#10-分组规则｜通过匿名函数2" class="headerlink" title="10. 分组规则｜通过匿名函数2"></a><strong>10. 分组规则｜通过匿名函数2</strong></h3><ul><li><strong>题目</strong>: 计算各行政区的企业领域（industryField）包含电商的总数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">ecom_counts = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;industryField&#x27;</span>].apply(<span class="hljs-keyword">lambda</span> x: x.<span class="hljs-built_in">str</span>.contains(<span class="hljs-string">&#x27;电商&#x27;</span>).<span class="hljs-built_in">sum</span>())<br><span class="hljs-built_in">print</span>(ecom_counts)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>.apply()</code> 是 <code>groupby</code> 中最灵活的方法，它可以接收一个自定义函数（这里用 <code>lambda</code> 匿名函数）。</li></ul><ol><li><code>df.groupby(&#39;district&#39;)[&#39;industryField&#39;]</code>: 首先，按区分组，并选取 <code>industryField</code> 列。</li><li><strong><code>.apply(lambda x: ...)</code></strong>: <code>apply</code> 会将每个区的 <code>industryField</code> Series（记为 <code>x</code>）作为参数传递给 <code>lambda</code> 函数。</li><li><strong><code>x.str.contains(&#39;电商&#39;)</code></strong>: 对每个组的 <code>industryField</code> 列（<code>x</code>），使用 <code>.str.contains(&#39;电商&#39;)</code> 判断每一行是否包含“电商”二字，返回一个布尔值的 Series（<code>True</code>&#x2F;<code>False</code>）。</li><li><strong><code>.sum()</code></strong>: 对上一步的布尔 Series 求和。在求和运算中，<code>True</code> 被视为 1，<code>False</code> 被视为 0。因此，<code>.sum()</code> 的结果就是该区包含“电商”的岗位总数。</li></ol></li></ul><h3 id="11-分组规则｜通过内置函数"><a href="#11-分组规则｜通过内置函数" class="headerlink" title="11. 分组规则｜通过内置函数"></a><strong>11. 分组规则｜通过内置函数</strong></h3><ul><li><strong>题目</strong>: 通过 <code>positionName</code> 的长度进行分组，并计算不同长度岗位名称的薪资均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">salary_by_len = df.groupby(df[<span class="hljs-string">&#x27;positionName&#x27;</span>].<span class="hljs-built_in">str</span>.<span class="hljs-built_in">len</span>())[<span class="hljs-string">&#x27;salary&#x27;</span>].mean()<br><span class="hljs-built_in">print</span>(salary_by_len)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>这与第9题类似，我们再次使用一个<strong>派生 Series</strong> 作为分组依据。</li></ul><ol><li><strong><code>df[&#39;positionName&#39;].str.len()</code></strong>: <code>.str.len()</code> 会计算 <code>positionName</code> 列中每个字符串的长度，生成一个长度与原DataFrame相同的 Series。</li><li><strong><code>df.groupby(...)</code></strong>: 使用这个长度 Series 作为 <code>groupby</code> 的键。</li><li><strong><code>[&#39;salary&#39;].mean()</code></strong>: 对每个“岗位名长度”组，计算其对应薪资的平均值。</li></ol></li></ul><h3 id="12-分组规则｜通过字典"><a href="#12-分组规则｜通过字典" class="headerlink" title="12. 分组规则｜通过字典"></a><strong>12. 分组规则｜通过字典</strong></h3><ul><li><strong>题目</strong>: 将 <code>score</code> 和 <code>matchScore</code> 的和记为总分，与 <code>salary</code> 列同时进行分组，并查看结果。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 定义分组映射规则的字典</span><br>mapping = &#123;<span class="hljs-string">&#x27;score&#x27;</span>: <span class="hljs-string">&#x27;总分&#x27;</span>, <span class="hljs-string">&#x27;matchScore&#x27;</span>: <span class="hljs-string">&#x27;总分&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>: <span class="hljs-string">&#x27;薪资&#x27;</span>&#125;<br><span class="hljs-comment"># 按列名进行分组</span><br>grouped_by_dict = df.T.groupby(mapping).<span class="hljs-built_in">sum</span>().T<br><span class="hljs-built_in">print</span>(grouped_by_dict.<span class="hljs-built_in">sum</span>().head()) <span class="hljs-comment"># 以sum聚合为例查看结果</span><br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>groupby</code> 不仅可以按行分组，也可以<strong>按列分组</strong>。通过设置 <code>axis=1</code> 来实现。</li></ul><ol><li><strong><code>mapping = &#123;...&#125;</code></strong>: 我们创建一个字典。字典的 <code>key</code> 是原始的列名，<code>value</code> 是我们想把它们分到的组名。这里 <code>score</code> 和 <code>matchScore</code> 都被分到了“总分”组，<code>salary</code> 被分到了“薪资”组。</li><li><strong><code>df.groupby(mapping, axis=1)</code></strong>: 将这个映射字典和 <code>axis=1</code> 传给 <code>groupby</code>。Pandas 就会按列进行分组。</li><li><code>.sum()</code>: 在每个列分组上进行聚合操作，例如求和。结果中，“总分”这一列的值就是原 <code>score</code> 和 <code>matchScore</code> 的和。</li></ol></li></ul><h3 id="13-分组规则｜通过多列"><a href="#13-分组规则｜通过多列" class="headerlink" title="13. 分组规则｜通过多列"></a><strong>13. 分组规则｜通过多列</strong></h3><ul><li><strong>题目</strong>: 计算不同 工作年限（<code>workYear</code>）和 学历（<code>education</code>）之间的薪资均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">mean_salary_by_exp_edu = df.groupby([<span class="hljs-string">&#x27;workYear&#x27;</span>, <span class="hljs-string">&#x27;education&#x27;</span>])[<span class="hljs-string">&#x27;salary&#x27;</span>].mean()<br><span class="hljs-built_in">print</span>(mean_salary_by_exp_edu)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>这与第4题是完全相同的逻辑，只是换了分组的列。</li><li>通过向 <code>groupby</code> 传递一个包含多个列名的列表 <code>[&#39;workYear&#39;, &#39;education&#39;]</code>，即可实现按“工作年限-学历”这个组合进行分组，并计算每个组合的平均薪资。</li></ul></li></ul><h3 id="14-分组转换｜transform"><a href="#14-分组转换｜transform" class="headerlink" title="14. 分组转换｜transform"></a><strong>14. 分组转换｜transform</strong></h3><ul><li><strong>题目</strong>: 在原数据框 df 新增一列，数值为该区的平均薪资水平。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df_transformed = df.copy() <span class="hljs-comment"># 创建副本进行操作</span><br>df_transformed[<span class="hljs-string">&#x27;区平均工资&#x27;</span>] = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].transform(<span class="hljs-string">&#x27;mean&#x27;</span>)<br><span class="hljs-built_in">print</span>(df_transformed[[<span class="hljs-string">&#x27;district&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>, <span class="hljs-string">&#x27;区平均工资&#x27;</span>]].head())<br></code></pre></td></tr></table></figure><ul><li><strong>解答过程</strong>:</li><li><code>transform</code> 是 <code>groupby</code> 的一个非常强大的方法，它与 <code>agg</code>&#x2F;<code>apply</code> 的核心区别在于：<code>transform</code> <strong>返回一个与原始数据形状相同的 Series 或 DataFrame</strong>。</li></ul><ol><li><code>df.groupby(&#39;district&#39;)[&#39;salary&#39;].transform(&#39;mean&#39;)</code>: 这个操作会先按区计算好每个区的平均薪资。</li><li>然后，它会将计算出的平均值“广播”回原始的行。例如，所有 <code>district</code> 为“西湖区”的行，它们在新列中的值都会是同一个数字——“西湖区”的平均薪资。</li><li>这使得我们可以直接将结果作为新列 <code>[&#39;区平均工资&#39;]</code> 添加回 DataFrame，用于后续的比较或计算。</li></ol></li></ul><h3 id="15-分组过滤｜filter"><a href="#15-分组过滤｜filter" class="headerlink" title="15. 分组过滤｜filter"></a><strong>15. 分组过滤｜filter</strong></h3><ul><li><strong>题目</strong>: 提取平均工资小于 30000 的行政区的全部数据。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">filtered_districts = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>).<span class="hljs-built_in">filter</span>(<span class="hljs-keyword">lambda</span> x: x[<span class="hljs-string">&#x27;salary&#x27;</span>].mean() &lt; <span class="hljs-number">30000</span>)<br><span class="hljs-built_in">print</span>(filtered_districts.head())<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>filter</code> 是另一个 <code>groupby</code> 的核心方法，用于<strong>按组的性质</strong>来筛选数据。</li></ul><ol><li><strong><code>.filter(lambda x: ...)</code></strong>: <code>filter</code> 会将每个分组（一个子 DataFrame，记为 <code>x</code>）传递给 <code>lambda</code> 函数。</li><li><strong><code>x[&#39;salary&#39;].mean() &lt; 30000</code></strong>: 这个函数必须返回一个布尔值（<code>True</code> 或 <code>False</code>）。这里我们计算每个组 <code>x</code> 的平均薪资，并判断是否小于 30000。</li><li>如果函数返回 <code>True</code>，那么这个组的<strong>所有原始行</strong>都会被保留下来。</li><li>如果函数返回 <code>False</code>，这个组的<strong>所有原始行</strong>都会被丢弃。</li><li>最终，<code>filter</code> 会返回一个由所有被保留的组拼接而成的 DataFrame。</li></ol></li></ul><h3 id="16-分组可视化"><a href="#16-分组可视化" class="headerlink" title="16. 分组可视化"></a><strong>16. 分组可视化</strong></h3><ul><li><strong>题目</strong>: 对杭州市各区公司数量进行分组，并使用柱状图进行可视化。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> matplotlib.pyplot <span class="hljs-keyword">as</span> plt<br><span class="hljs-comment"># 解决中文显示问题</span><br>plt.rcParams[<span class="hljs-string">&#x27;font.sans-serif&#x27;</span>] = [<span class="hljs-string">&#x27;SimHei&#x27;</span>]<br>plt.rcParams[<span class="hljs-string">&#x27;axes.unicode_minus&#x27;</span>] = <span class="hljs-literal">False</span><br><br>district_counts = df[<span class="hljs-string">&#x27;district&#x27;</span>].value_counts()<br>district_counts.plot(kind=<span class="hljs-string">&#x27;bar&#x27;</span>, figsize=(<span class="hljs-number">12</span>, <span class="hljs-number">6</span>), title=<span class="hljs-string">&#x27;杭州市各区招聘岗位数量&#x27;</span>)<br>plt.ylabel(<span class="hljs-string">&#x27;岗位数量&#x27;</span>)<br>plt.xlabel(<span class="hljs-string">&#x27;行政区&#x27;</span>)<br>plt.xticks(rotation=<span class="hljs-number">45</span>)<br>plt.show()<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ol><li><strong><code>df[&#39;district&#39;].value_counts()</code></strong>: 这是统计各区出现次数（即岗位数量）最快的方法。它会返回一个按数量降序排列的 Series。</li><li><strong><code>.plot(kind=&#39;bar&#39;, ...)</code></strong>: Pandas 的 Series 和 DataFrame 对象内置了 <code>.plot()</code> 方法，可以方便地快速绘图（底层是 Matplotlib）。</li></ol><ul><li><code>kind=&#39;bar&#39;</code> 指定了图表类型为柱状图。</li><li><code>figsize</code> 设置图表大小。</li><li><code>title</code> 设置图表标题。</li></ul><ol start="3"><li><strong><code>plt.ylabel/xlabel/xticks</code></strong>: 使用 <code>matplotlib.pyplot</code> 的函数来进一步美化图表，如添加坐标轴标签、旋转x轴刻度标签以防重叠。</li><li><strong><code>plt.show()</code></strong>: 显示图表。</li></ol></li></ul><hr><h2 id="聚合"><a href="#聚合" class="headerlink" title="聚合"></a><strong>聚合</strong></h2><p>聚合（Aggregation）是指对分组后的数据进行汇总计算，如求和、求均值等。<code>.agg()</code> 是实现聚合操作最核心、最灵活的函数。</p><h3 id="17-聚合统计"><a href="#17-聚合统计" class="headerlink" title="17. 聚合统计"></a><strong>17. 聚合统计</strong></h3><ul><li><strong>题目</strong>: 分组计算不同行政区，薪水的最小值、最大值和平均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">salary_stats = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].agg([<span class="hljs-string">&#x27;min&#x27;</span>, <span class="hljs-string">&#x27;max&#x27;</span>, <span class="hljs-string">&#x27;mean&#x27;</span>])<br><span class="hljs-built_in">print</span>(salary_stats)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><strong><code>.agg()</code></strong>: 当你想对一个分组应用<strong>多个聚合函数</strong>时，<code>.agg()</code> 是最佳选择。</li><li>将一个包含函数名称字符串的列表 <code>[&#39;min&#39;, &#39;max&#39;, &#39;mean&#39;]</code> 传递给 <code>.agg()</code>，它就会为每个区计算出这三个统计量，并生成一个以这些统计量名称为列名的 DataFrame。</li></ul></li></ul><h3 id="18-聚合统计｜修改列名"><a href="#18-聚合统计｜修改列名" class="headerlink" title="18. 聚合统计｜修改列名"></a><strong>18. 聚合统计｜修改列名</strong></h3><ul><li><strong>题目</strong>: 将上一题的列名（包括索引名）修改为中文。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先得到上一题的结果</span><br>salary_stats = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].agg([<span class="hljs-string">&#x27;min&#x27;</span>, <span class="hljs-string">&#x27;max&#x27;</span>, <span class="hljs-string">&#x27;mean&#x27;</span>])<br><span class="hljs-comment"># 使用 rename 方法修改</span><br>salary_stats_renamed = salary_stats.rename(<br>    columns=&#123;<span class="hljs-string">&#x27;min&#x27;</span>: <span class="hljs-string">&#x27;最低工资&#x27;</span>, <span class="hljs-string">&#x27;max&#x27;</span>: <span class="hljs-string">&#x27;最高工资&#x27;</span>, <span class="hljs-string">&#x27;mean&#x27;</span>: <span class="hljs-string">&#x27;平均工资&#x27;</span>&#125;,<br>    index=&#123;<span class="hljs-string">&#x27;district&#x27;</span>: <span class="hljs-string">&#x27;行政区&#x27;</span>&#125; <span class="hljs-comment"># rename也可以修改索引名</span><br>)<br>salary_stats_renamed.index.name = <span class="hljs-string">&#x27;行政区&#x27;</span> <span class="hljs-comment"># 或者用这种方式修改索引名</span><br><span class="hljs-built_in">print</span>(salary_stats_renamed)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>rename()</code> 方法既可以修改列名（通过 <code>columns</code> 参数），也可以修改索引标签（通过 <code>index</code> 参数）。</li><li>分别传入一个“原名:新名”的字典即可。</li><li>对于索引名，也可以像第5题一样，通过 <code>.index.name</code> 属性直接赋值。</li></ul></li></ul><h3 id="19-聚合统计｜组合"><a href="#19-聚合统计｜组合" class="headerlink" title="19. 聚合统计｜组合"></a><strong>19. 聚合统计｜组合</strong></h3><ul><li><strong>题目</strong>: 对不同岗位(<code>positionName</code>)进行分组，并统计其薪水(<code>salary</code>)中位数和得分(<code>score</code>)均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs python">  position_stats = df.groupby(<span class="hljs-string">&#x27;positionName&#x27;</span>).agg(<br>      薪水中位数=(<span class="hljs-string">&#x27;salary&#x27;</span>, <span class="hljs-string">&#x27;median&#x27;</span>),<br>      得分均值=(<span class="hljs-string">&#x27;score&#x27;</span>, <span class="hljs-string">&#x27;mean&#x27;</span>)<br>  )<br>  <span class="hljs-built_in">print</span>(position_stats.head())<br>  ```- **解答过程**:<br>  - 这是 `.agg()` 最强大、最推荐的用法：**命名聚合 (Named Aggregation)**。<br>  - 语法是 `agg(新列名=(<span class="hljs-string">&#x27;操作的原始列&#x27;</span>, <span class="hljs-string">&#x27;聚合函数&#x27;</span>))`。<br>  - 这种方式可以一次性对**不同的列**应用**不同的聚合函数**，并且直接生成表意清晰的**新列名**，非常高效和优雅。<br><br><span class="hljs-comment">### **20. 聚合统计｜多层**</span><br>- **题目**: 对不同行政区进行分组，并统计薪水的均值、中位数、方差，以及得分的均值。<br>- **答案**:<br>  ```python<br>  district_multi_stats = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>).agg(&#123;<br>      <span class="hljs-string">&#x27;salary&#x27;</span>: [<span class="hljs-string">&#x27;mean&#x27;</span>, <span class="hljs-string">&#x27;median&#x27;</span>, <span class="hljs-string">&#x27;std&#x27;</span>],<br>      <span class="hljs-string">&#x27;score&#x27;</span>: <span class="hljs-string">&#x27;mean&#x27;</span><br>  &#125;)<br>  <span class="hljs-built_in">print</span>(district_multi_stats)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li>当需要对多个列应用不同的聚合函数组合时，可以向 <code>.agg()</code> 传递一个字典。</li><li><strong>字典的键 (key)</strong> 是要操作的列名，如 <code>&#39;salary&#39;</code>。</li><li><strong>字典的值 (value)</strong> 是要应用的聚合函数。如果对一列应用多个函数，值就是一个函数列表，如 <code>[&#39;mean&#39;, &#39;median&#39;, &#39;std&#39;]</code>；如果只应用一个，值就是函数名字符串，如 <code>&#39;mean&#39;</code>。</li><li>结果会生成一个**多级列索引 (MultiIndex column)**。</li></ul></li></ul><h3 id="21-聚合统计｜自定义函数"><a href="#21-聚合统计｜自定义函数" class="headerlink" title="21. 聚合统计｜自定义函数"></a><strong>21. 聚合统计｜自定义函数</strong></h3><ul><li><strong>题目</strong>: 在 18 题基础上，在聚合计算时新增一列计算最大值与平均值的差值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 定义一个自定义函数</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">max_mean_diff</span>(<span class="hljs-params">x</span>):<br>    <span class="hljs-keyword">return</span> x.<span class="hljs-built_in">max</span>() - x.mean()<br><br>salary_custom_agg = df.groupby(<span class="hljs-string">&#x27;district&#x27;</span>)[<span class="hljs-string">&#x27;salary&#x27;</span>].agg(<br>    最低工资=<span class="hljs-string">&#x27;min&#x27;</span>,<br>    最高工资=<span class="hljs-string">&#x27;max&#x27;</span>,<br>    平均工资=<span class="hljs-string">&#x27;mean&#x27;</span>,<br>    最大值与均值差值=max_mean_diff<br>)<br><span class="hljs-built_in">print</span>(salary_custom_agg)<br></code></pre></td></tr></table></figure></li><li><strong>解答过程</strong>:<ul><li><code>.agg()</code> 不仅能接受内置的函数名字符串，还能接受<strong>用户自己定义的函数</strong>。</li></ul><ol><li>我们先定义一个函数 <code>max_mean_diff</code>，它接收一个 Series (<code>x</code>)，并返回其最大值与平均值的差。</li><li>在 <code>.agg()</code> 的命名聚合中，我们直接将这个<strong>函数对象</strong> <code>max_mean_diff</code> 作为聚合函数传入。Pandas 会自动将每个区的 <code>salary</code> Series 传给这个函数并计算结果。</li><li>使用 <code>lambda</code> 匿名函数也可以实现同样的效果：<code>最大值与均值差值=lambda x: x.max() - x.mean()</code>。这使得 <code>.agg()</code> 的功能几乎是无限的。</li></ol></li></ul>]]></content>
      
      
      <categories>
          
          <category> pandas </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pandas </tag>
            
            <tag> 数据处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>5.数据筛选与修改</title>
      <link href="/2025/5-%E6%95%B0%E6%8D%AE%E7%AD%9B%E9%80%89%E4%B8%8E%E4%BF%AE%E6%94%B9/"/>
      <url>/2025/5-%E6%95%B0%E6%8D%AE%E7%AD%9B%E9%80%89%E4%B8%8E%E4%BF%AE%E6%94%B9/</url>
      
        <content type="html"><![CDATA[<h3 id="初始化与加载数据"><a href="#初始化与加载数据" class="headerlink" title="初始化与加载数据"></a><strong>初始化与加载数据</strong></h3><p>首先，我们导入 <code>pandas</code> 并加载东京奥运会的奖牌数据。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> pandas <span class="hljs-keyword">as</span> pd<br><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np <span class="hljs-comment"># 导入numpy，后面会用到</span><br><br><span class="hljs-comment"># 从网页直接读取数据，Pandas会自动抓取页面上的表格</span><br><span class="hljs-comment"># read_html返回一个列表，我们取第一个表格[0]</span><br>df = pd.read_html(<span class="hljs-string">&quot;https://olympics.com/tokyo-2020/olympic-games/zh/results/all-sports/medal-standings.htm&quot;</span>)[<span class="hljs-number">0</span>]<br><br><span class="hljs-comment"># 如果网络不稳定，也可以加载本地文件</span><br><span class="hljs-comment"># df = pd.read_csv(&quot;东京奥运会奖牌数据.csv&quot;)</span><br></code></pre></td></tr></table></figure><hr><h2 id="5-1-数据修改"><a href="#5-1-数据修改" class="headerlink" title="5-1 数据修改"></a><strong>5-1 数据修改</strong></h2><h3 id="1-数据修改｜列名"><a href="#1-数据修改｜列名" class="headerlink" title="1. 数据修改｜列名"></a><strong>1. 数据修改｜列名</strong></h3><ul><li><strong>题目</strong>: 将原 <code>df</code> 列名 <code>Unnamed: 2</code>、<code>Unnamed: 3</code>、<code>Unnamed: 4</code> 修改为 <code>金牌数</code>、<code>银牌数</code>、<code>铜牌数</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python">df_renamed = df.rename(columns=&#123;<span class="hljs-string">&#x27;Unnamed: 2&#x27;</span>: <span class="hljs-string">&#x27;金牌数&#x27;</span>,<br>                                <span class="hljs-string">&#x27;Unnamed: 3&#x27;</span>: <span class="hljs-string">&#x27;银牌数&#x27;</span>,<br>                                <span class="hljs-string">&#x27;Unnamed: 4&#x27;</span>: <span class="hljs-string">&#x27;铜牌数&#x27;</span>&#125;)<br><span class="hljs-comment"># 查看修改后的前几行</span><br>df_renamed.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>rename()</code> 是用于修改轴标签（行索引或列名）的函数。</li><li><code>columns=&#123;...&#125;</code> 参数接收一个字典，<code>key</code> 是原始列名，<code>value</code> 是你想要的新列名。</li></ul></li></ul><h3 id="2-数据修改｜行索引"><a href="#2-数据修改｜行索引" class="headerlink" title="2. 数据修改｜行索引"></a><strong>2. 数据修改｜行索引</strong></h3><ul><li><strong>题目</strong>: 将第一列（排名）设置为索引。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 在上一步重命名后的df_renamed上操作</span><br>df_indexed = df_renamed.set_index(<span class="hljs-string">&#x27;排名&#x27;</span>)<br>df_indexed.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>set_index()</code> 方法可以将 DataFrame 的某一列或多列设置为新的行索引。这在后续使用 <code>.loc</code> 按标签查找时非常方便。</li></ul></li></ul><h3 id="3-数据修改｜修改索引名"><a href="#3-数据修改｜修改索引名" class="headerlink" title="3. 数据修改｜修改索引名"></a><strong>3. 数据修改｜修改索引名</strong></h3><ul><li><strong>题目</strong>: 修改索引名为 金牌排名。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 同样在上一步的结果上操作</span><br>df_named_index = df_indexed.copy() <span class="hljs-comment"># 创建副本以避免修改df_indexed</span><br>df_named_index.index.name = <span class="hljs-string">&#x27;金牌排名&#x27;</span><br>df_named_index.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>通过访问 <code>df.index.name</code> 属性，可以直接为其赋值，从而修改索引的名称。</li></ul></li></ul><h3 id="4-数据修改｜修改值"><a href="#4-数据修改｜修改值" class="headerlink" title="4. 数据修改｜修改值"></a><strong>4. 数据修改｜修改值</strong></h3><ul><li><strong>题目</strong>: 将 ROC（第一列第五行）修改为 俄奥委会。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">df_modified_value = df_named_index.copy()<br><span class="hljs-comment"># 使用 .loc[行标签, 列标签] 定位并修改</span><br>df_modified_value.loc[<span class="hljs-number">5</span>, <span class="hljs-string">&#x27;国家奥委会&#x27;</span>] = <span class="hljs-string">&#x27;俄奥委会&#x27;</span><br>df_modified_value.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.loc</code> 是基于<strong>标签</strong>进行索引的，是我们修改值的首选方法。<code>df.loc[行标签, 列标签] = 新值</code> 可以精确地定位到单元格并赋予新值。</li></ul></li></ul><h3 id="5-数据修改｜替换值（单值）"><a href="#5-数据修改｜替换值（单值）" class="headerlink" title="5. 数据修改｜替换值（单值）"></a><strong>5. 数据修改｜替换值（单值）</strong></h3><ul><li><strong>题目</strong>: 将金牌数列的数字 <code>0</code> 替换为 <code>无</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 创建一个副本进行操作</span><br>df_replaced = df_modified_value.copy()<br>df_replaced[<span class="hljs-string">&#x27;金牌数&#x27;</span>] = df_replaced[<span class="hljs-string">&#x27;金牌数&#x27;</span>].replace(<span class="hljs-number">0</span>, <span class="hljs-string">&#x27;无&#x27;</span>)<br>df_replaced.tail() <span class="hljs-comment"># 查看尾部数据确认替换</span><br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>replace()</code> 方法可以替换 Series 或 DataFrame 中的值。<code>replace(要被替换的值, 新值)</code> 是其基本用法。</li></ul></li></ul><h3 id="6-数据修改｜替换值（多值）"><a href="#6-数据修改｜替换值（多值）" class="headerlink" title="6. 数据修改｜替换值（多值）"></a><strong>6. 数据修改｜替换值（多值）</strong></h3><ul><li><strong>题目</strong>: 同时替换，将 <code>无</code> 替换为缺失值，将 <code>0</code> 替换为 <code>None</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 使用上一步的 df_replaced 进行操作</span><br>df_multi_replaced = df_replaced.replace(&#123;<span class="hljs-string">&#x27;无&#x27;</span>: np.nan, <span class="hljs-number">0</span>: <span class="hljs-literal">None</span>&#125;)<br>df_multi_replaced.tail()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>replace()</code> 也可以接收一个字典来进行多值替换。<code>key</code> 是待替换的值，<code>value</code> 是新值。</li><li><code>np.nan</code> 是 <code>NumPy</code> 库中表示标准缺失值的对象 (Not a Number)。Pandas 中 <code>None</code> 也会被视作缺失值。</li></ul></li></ul><h3 id="7-数据查看"><a href="#7-数据查看" class="headerlink" title="7. 数据查看"></a><strong>7. 数据查看</strong></h3><ul><li><strong>题目</strong>: 查看各列数据类型。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_multi_replaced.dtypes<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.dtypes</code> 属性可以快速返回 DataFrame 中每一列的数据类型。</li></ul></li></ul><h3 id="8-数据修改｜修改类型"><a href="#8-数据修改｜修改类型" class="headerlink" title="8. 数据修改｜修改类型"></a><strong>8. 数据修改｜修改类型</strong></h3><ul><li><strong>题目</strong>: 将 <code>金牌数</code> 列类型修改为 <code>int</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment">#  由于上一步我们引入了np.nan(浮点型)，直接转int会报错</span><br><span class="hljs-comment">#  需要先填充缺失值</span><br>df_temp = df_multi_replaced.copy()<br>df_temp[<span class="hljs-string">&#x27;金牌数&#x27;</span>] = df_temp[<span class="hljs-string">&#x27;金牌数&#x27;</span>].fillna(<span class="hljs-number">0</span>) <span class="hljs-comment"># 将缺失值填充为0</span><br><br><span class="hljs-comment"># 现在可以安全地转换类型了</span><br>df_temp[<span class="hljs-string">&#x27;金牌数&#x27;</span>] = df_temp[<span class="hljs-string">&#x27;金牌数&#x27;</span>].astype(<span class="hljs-built_in">int</span>)<br><span class="hljs-built_in">print</span>(df_temp.dtypes)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.astype()</code> 是转换数据类型的核心方法。</li><li><strong>注意</strong>：标准的 <code>int</code> 类型不支持缺失值 <code>np.nan</code>。在转换前，必须确保该列不含缺失值（可以填充或删除）。如果你想保留缺失值的同时使用整数类型，可以使用 Pandas 提供的 nullable integer 类型：<code>astype(&#39;Int64&#39;)</code>（注意大写的’I’）。</li></ul></li></ul><h3 id="9-数据增加｜新增列（固定值）"><a href="#9-数据增加｜新增列（固定值）" class="headerlink" title="9. 数据增加｜新增列（固定值）"></a><strong>9. 数据增加｜新增列（固定值）</strong></h3><ul><li><strong>题目</strong>: <strong>重新加载数据</strong> 并 新增一列 <code>比赛地点</code>，值为 <code>东京</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 重新加载并重命名</span><br>df_reloaded = df.rename(columns=&#123;<span class="hljs-string">&#x27;Unnamed: 2&#x27;</span>: <span class="hljs-string">&#x27;金牌数&#x27;</span>, <span class="hljs-string">&#x27;Unnamed: 3&#x27;</span>: <span class="hljs-string">&#x27;银牌数&#x27;</span>, <span class="hljs-string">&#x27;Unnamed: 4&#x27;</span>: <span class="hljs-string">&#x27;铜牌数&#x27;</span>&#125;)<br><br>df_new_col = df_reloaded.copy()<br>df_new_col[<span class="hljs-string">&#x27;比赛地点&#x27;</span>] = <span class="hljs-string">&#x27;东京&#x27;</span><br>df_new_col.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>新增一列最简单的方法就是像字典一样直接赋值：<code>df[&#39;新列名&#39;] = 值</code>。Pandas 会自动将这个值广播到每一行。</li></ul></li></ul><h3 id="10-数据增加｜新增列（计算值）"><a href="#10-数据增加｜新增列（计算值）" class="headerlink" title="10. 数据增加｜新增列（计算值）"></a><strong>10. 数据增加｜新增列（计算值）</strong></h3><ul><li><strong>题目</strong>: 新增一列 <code>金银牌总数</code> 列，值为该国家金银牌总数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df_calc_col = df_new_col.copy()<br>df_calc_col[<span class="hljs-string">&#x27;金银牌总数&#x27;</span>] = df_calc_col[<span class="hljs-string">&#x27;金牌数&#x27;</span>] + df_calc_col[<span class="hljs-string">&#x27;银牌数&#x27;</span>]<br>df_calc_col.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>新增的列可以是现有列之间计算的结果。Pandas 支持列与列之间的向量化运算，非常高效。</li></ul></li></ul><h3 id="11-数据增加｜新增列（比较值）"><a href="#11-数据增加｜新增列（比较值）" class="headerlink" title="11. 数据增加｜新增列（比较值）"></a><strong>11. 数据增加｜新增列（比较值）</strong></h3><ul><li><strong>题目</strong>: 新增一列 <code>最多奖牌数量</code> 列，值为该过金银牌数量种最多的一个奖牌数量。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df_max_col = df_calc_col.copy()<br>df_max_col[<span class="hljs-string">&#x27;最多奖牌数量&#x27;</span>] = df_max_col[[<span class="hljs-string">&#x27;金牌数&#x27;</span>, <span class="hljs-string">&#x27;银牌数&#x27;</span>, <span class="hljs-string">&#x27;铜牌数&#x27;</span>]].<span class="hljs-built_in">max</span>(axis=<span class="hljs-number">1</span>)<br>df_max_col.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.max()</code> 方法可以求最大值。当应用于多列组成的 DataFrame 时，<code>axis=1</code> 参数表示<strong>按行操作</strong>，即计算每一行中指定列的最大值。</li></ul></li></ul><h3 id="12-数据增加｜新增列（判断值）"><a href="#12-数据增加｜新增列（判断值）" class="headerlink" title="12. 数据增加｜新增列（判断值）"></a><strong>12. 数据增加｜新增列（判断值）</strong></h3><ul><li><strong>题目</strong>: 新增一列 <code>金牌大于30</code>，如果金牌数大于 30 则值为 <code>是</code>，反之为 <code>否</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df_cond_col = df_max_col.copy()<br>df_cond_col[<span class="hljs-string">&#x27;金牌大于30&#x27;</span>] = np.where(df_cond_col[<span class="hljs-string">&#x27;金牌数&#x27;</span>] &gt; <span class="hljs-number">30</span>, <span class="hljs-string">&#x27;是&#x27;</span>, <span class="hljs-string">&#x27;否&#x27;</span>)<br>df_cond_col.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>np.where(condition, value_if_true, value_if_false)</code> 是实现条件赋值的绝佳工具。它会根据 <code>condition</code> 的布尔结果，从后面两个参数中选择对应的值，效率远高于 <code>.apply</code>。</li></ul></li></ul><h3 id="13-数据增加｜增加多列"><a href="#13-数据增加｜增加多列" class="headerlink" title="13. 数据增加｜增加多列"></a><strong>13. 数据增加｜增加多列</strong></h3><ul><li><strong>题目</strong>: 新增 <code>金铜牌总数</code> 和 <code>银铜牌总数</code> 两列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python">df_multi_add = df_cond_col.copy()<br>df_assigned = df_multi_add.assign(<br>    金铜牌总数 = df_multi_add[<span class="hljs-string">&#x27;金牌数&#x27;</span>] + df_multi_add[<span class="hljs-string">&#x27;铜牌数&#x27;</span>],<br>    银铜牌总数 = df_multi_add[<span class="hljs-string">&#x27;银牌数&#x27;</span>] + df_multi_add[<span class="hljs-string">&#x27;铜牌数&#x27;</span>]<br>)<br>df_assigned.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>assign()</code> 方法允许链式调用，一次性创建多个新列，代码更整洁。它会返回一个包含新列的<strong>新</strong> DataFrame。</li></ul></li></ul><h3 id="14-数据增加｜新增列（引用变量）"><a href="#14-数据增加｜新增列（引用变量）" class="headerlink" title="14. 数据增加｜新增列（引用变量）"></a><strong>14. 数据增加｜新增列（引用变量）</strong></h3><ul><li><strong>题目</strong>: 新增一列 <code>金牌占比</code>，为各国金牌数除以总金牌数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先计算总金牌数</span><br>gold_sum = df_assigned[<span class="hljs-string">&#x27;金牌数&#x27;</span>].<span class="hljs-built_in">sum</span>()<br><br><span class="hljs-comment"># 再计算占比</span><br>df_ratio = df_assigned.copy()<br>df_ratio[<span class="hljs-string">&#x27;金牌占比&#x27;</span>] = df_ratio[<span class="hljs-string">&#x27;金牌数&#x27;</span>] / gold_sum<br>df_ratio.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>新增列的计算过程可以引用外部变量（如 <code>gold_sum</code>），这在进行标准化或计算占比等场景中非常常见。</li></ul></li></ul><h3 id="15-数据增加｜新增行（末尾追加）"><a href="#15-数据增加｜新增行（末尾追加）" class="headerlink" title="15. 数据增加｜新增行（末尾追加）"></a><strong>15. 数据增加｜新增行（末尾追加）</strong></h3><ul><li><strong>题目</strong>: 在 df 末尾追加一行，内容为 0,1,2,3…</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 创建新行的数据</span><br>new_row_data = <span class="hljs-built_in">list</span>(<span class="hljs-built_in">range</span>(df_ratio.shape[<span class="hljs-number">1</span>]))<br><span class="hljs-comment"># 将新行包装成一个DataFrame</span><br>new_row_df = pd.DataFrame([new_row_data], columns=df_ratio.columns)<br><br><span class="hljs-comment"># 使用pd.concat合并</span><br>df_appended = pd.concat([df_ratio, new_row_df], ignore_index=<span class="hljs-literal">True</span>)<br>df_appended.tail()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>自 Pandas 2.0 起，官方推荐使用 <code>pd.concat</code> 来合并 DataFrame，以取代已弃用的 <code>append</code> 方法。</li><li><code>ignore_index=True</code> 会创建一个新的、从0开始的连续索引。</li></ul></li></ul><h3 id="16-数据增加｜新增行（指定位置）"><a href="#16-数据增加｜新增行（指定位置）" class="headerlink" title="16. 数据增加｜新增行（指定位置）"></a><strong>16. 数据增加｜新增行（指定位置）</strong></h3><ul><li><strong>题目</strong>: 在第 2 行（美国和中国之间）新增一行数据。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 定义新行</span><br>new_row_df = pd.DataFrame([<span class="hljs-built_in">list</span>(<span class="hljs-built_in">range</span>(df_ratio.shape[<span class="hljs-number">1</span>]))], columns=df_ratio.columns)<br><br><span class="hljs-comment"># 将原DataFrame拆分为上下两部分</span><br>df_top = df_ratio.iloc[:<span class="hljs-number">2</span>]<br>df_bottom = df_ratio.iloc[<span class="hljs-number">2</span>:]<br><br><span class="hljs-comment"># 按顺序合并</span><br>df_inserted = pd.concat([df_top, new_row_df, df_bottom], ignore_index=<span class="hljs-literal">True</span>)<br>df_inserted.head(<span class="hljs-number">4</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>在 DataFrame 中间插入行的标准做法是将其“切开”，然后将顶部、新行、底部按顺序用 <code>pd.concat</code> “粘合”起来。</li></ul></li></ul><h3 id="17-数据删除｜删除行"><a href="#17-数据删除｜删除行" class="headerlink" title="17. 数据删除｜删除行"></a><strong>17. 数据删除｜删除行</strong></h3><ul><li><strong>题目</strong>: 删除 <code>df</code> 第一行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 使用插入后的df_inserted</span><br>df_dropped_row = df_inserted.drop(index=<span class="hljs-number">0</span>)<br>df_dropped_row.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>drop()</code> 是通用的删除函数。<code>index=...</code> 参数用于指定要删除的行的索引标签。</li></ul></li></ul><h3 id="18-数据删除｜删除行（条件）"><a href="#18-数据删除｜删除行（条件）" class="headerlink" title="18. 数据删除｜删除行（条件）"></a><strong>18. 数据删除｜删除行（条件）</strong></h3><ul><li><strong>题目</strong>: 删除金牌数为 0 的所有行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先筛选出金牌数为0的行的索引</span><br>indices_to_drop = df_dropped_row[df_dropped_row[<span class="hljs-string">&#x27;金牌数&#x27;</span>] == <span class="hljs-number">0</span>].index<br><br><span class="hljs-comment"># 再根据索引删除</span><br>df_dropped_cond = df_dropped_row.drop(index=indices_to_drop)<br>df_dropped_cond.tail()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>条件删除通常分两步：1. 根据你的条件找到需要删除的行的索引。2. 将这些索引用 <code>drop()</code> 方法删除。</li></ul></li></ul><h3 id="19-数据删除｜删除列"><a href="#19-数据删除｜删除列" class="headerlink" title="19. 数据删除｜删除列"></a><strong>19. 数据删除｜删除列</strong></h3><ul><li><strong>题目</strong>: 删除刚刚新增的 <code>比赛地点</code> 列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 使用上一个结果</span><br>df_dropped_col = df_dropped_cond.drop(columns=[<span class="hljs-string">&#x27;比赛地点&#x27;</span>])<br>df_dropped_col.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>drop()</code> 方法的 <code>columns=...</code> 参数用于指定要删除的列的名称。</li></ul></li></ul><h3 id="20-数据删除｜删除列（按列号）"><a href="#20-数据删除｜删除列（按列号）" class="headerlink" title="20. 数据删除｜删除列（按列号）"></a><strong>20. 数据删除｜删除列（按列号）</strong></h3><ul><li><strong>题目</strong>: 删除 <code>df</code> 的 <code>7、8、9、10</code> 列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">cols_to_drop = df_dropped_col.columns[[<span class="hljs-number">7</span>, <span class="hljs-number">8</span>, <span class="hljs-number">9</span>, <span class="hljs-number">10</span>]]<br>df_dropped_by_pos = df_dropped_col.drop(columns=cols_to_drop)<br>df_dropped_by_pos.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>drop</code> 不直接支持按列号删除。你需要先通过 <code>df.columns[...]</code> 按位置获取列名，然后再按列名删除。</li></ul></li></ul><hr><h2 id="5-2-数据筛选"><a href="#5-2-数据筛选" class="headerlink" title="5-2 数据筛选"></a><strong>5-2 数据筛选</strong></h2><h3 id="21-重新加载数据"><a href="#21-重新加载数据" class="headerlink" title="21. 重新加载数据"></a><strong>21. 重新加载数据</strong></h3><ul><li><strong>操作</strong>: 为了这部分的练习，我们重新加载一份干净的数据。<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df = pd.read_html(<span class="hljs-string">&quot;https://olympics.com/tokyo-2020/olympic-games/zh/results/all-sports/medal-standings.htm&quot;</span>)[<span class="hljs-number">0</span>]<br>df.rename(columns=&#123;<span class="hljs-string">&#x27;Unnamed: 2&#x27;</span>:<span class="hljs-string">&#x27;金牌数&#x27;</span>,<span class="hljs-string">&#x27;Unnamed: 3&#x27;</span>:<span class="hljs-string">&#x27;银牌数&#x27;</span>,<span class="hljs-string">&#x27;Unnamed: 4&#x27;</span>:<span class="hljs-string">&#x27;铜牌数&#x27;</span>&#125;, inplace=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure><strong>注意</strong>: 以下操作均基于这份新加载的 <code>df</code>。</li></ul><h3 id="22-筛选列｜通过列号"><a href="#22-筛选列｜通过列号" class="headerlink" title="22. 筛选列｜通过列号"></a><strong>22. 筛选列｜通过列号</strong></h3><ul><li><strong>题目</strong>: 提取第 1、2、3、4 列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_selected_cols = df.iloc[:, [<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]]<br>df_selected_cols.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>iloc</code> 是基于<strong>整数位置</strong>进行索引的。<code>:</code> 表示选择所有行，<code>[0, 1, 2, 3]</code> 表示选择指定的列。</li></ul></li></ul><h3 id="23-筛选列｜通过列名"><a href="#23-筛选列｜通过列名" class="headerlink" title="23. 筛选列｜通过列名"></a><strong>23. 筛选列｜通过列名</strong></h3><ul><li><strong>题目</strong>: 提取 <code>金牌数、银牌数、铜牌数</code> 三列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_selected_names = df[[<span class="hljs-string">&#x27;金牌数&#x27;</span>, <span class="hljs-string">&#x27;银牌数&#x27;</span>, <span class="hljs-string">&#x27;铜牌数&#x27;</span>]]<br>df_selected_names.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>当你向 <code>df[...]</code> 中传入一个列名列表时，就可以选取多列。<strong>使用双中括号 <code>[[...]]</code> 会确保返回的是一个 DataFrame</strong>，而不是 Series。</li></ul></li></ul><h3 id="24-筛选列｜条件（列号）"><a href="#24-筛选列｜条件（列号）" class="headerlink" title="24. 筛选列｜条件（列号）"></a><strong>24. 筛选列｜条件（列号）</strong></h3><ul><li><strong>题目</strong>: 筛选全部 <strong>奇数列</strong>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_odd_cols = df.iloc[:, <span class="hljs-number">1</span>::<span class="hljs-number">2</span>]<br>df_odd_cols.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>iloc</code> 支持 Python 的切片语法。<code>1::2</code> 的意思是：从索引 1 开始，到结尾为止，步长为 2。</li></ul></li></ul><h3 id="25-筛选列｜条件（列名）"><a href="#25-筛选列｜条件（列名）" class="headerlink" title="25. 筛选列｜条件（列名）"></a><strong>25. 筛选列｜条件（列名）</strong></h3><ul><li><strong>题目</strong>: 提取全部列名中包含 <code>数</code> 的列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_filtered_names = df.<span class="hljs-built_in">filter</span>(like=<span class="hljs-string">&#x27;数&#x27;</span>)<br>df_filtered_names.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>filter()</code> 方法是筛选列名的利器。<code>like=&#39;数&#39;</code> 会返回所有列名中包含“数”这个字的列。</li></ul></li></ul><h3 id="26-筛选列｜组合（行号-列名）"><a href="#26-筛选列｜组合（行号-列名）" class="headerlink" title="26. 筛选列｜组合（行号+列名）"></a><strong>26. 筛选列｜组合（行号+列名）</strong></h3><ul><li><strong>题目</strong>: 提取倒数后三列的10-20行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_sliced = df.iloc[<span class="hljs-number">10</span>:<span class="hljs-number">21</span>, -<span class="hljs-number">3</span>:]<br>df_sliced<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>iloc</code> 非常适合这种行列组合切片。<code>10:21</code> 选取第10到20行（不含21），<code>-3:</code> 选取倒数第3列到最后一列。</li></ul></li></ul><h3 id="27-筛选行｜通过行号"><a href="#27-筛选行｜通过行号" class="headerlink" title="27. 筛选行｜通过行号"></a><strong>27. 筛选行｜通过行号</strong></h3><ul><li><strong>题目</strong>: 提取第 10 行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_row_10 = df.iloc[[<span class="hljs-number">9</span>]] <span class="hljs-comment"># 索引从0开始，所以第10行是iloc[9]</span><br>df_row_10<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>同样，使用双中括号 <code>[[9]]</code> 会返回一个单行 DataFrame。如果用 <code>df.iloc[9]</code> 则会返回一个 Series。</li></ul></li></ul><h3 id="28-筛选行｜通过行号（多行）"><a href="#28-筛选行｜通过行号（多行）" class="headerlink" title="28. 筛选行｜通过行号（多行）"></a><strong>28. 筛选行｜通过行号（多行）</strong></h3><ul><li><strong>题目</strong>: 提取第 10 行之后的全部行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_after_10 = df.iloc[<span class="hljs-number">10</span>:]<br>df_after_10.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>切片 <code>10:</code> 表示从索引为10的行开始，一直到 DataFrame 的末尾。</li></ul></li></ul><h3 id="29-筛选行｜固定间隔"><a href="#29-筛选行｜固定间隔" class="headerlink" title="29. 筛选行｜固定间隔"></a><strong>29. 筛选行｜固定间隔</strong></h3><ul><li><strong>题目</strong>: 提取 0-50 行，间隔为 3。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_strided = df.iloc[<span class="hljs-number">0</span>:<span class="hljs-number">51</span>:<span class="hljs-number">3</span>]<br>df_strided.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>Python 标准的切片 <code>[start:stop:step]</code> 在 <code>iloc</code> 中同样适用。</li></ul></li></ul><h3 id="30-筛选行｜判断（大于）"><a href="#30-筛选行｜判断（大于）" class="headerlink" title="30. 筛选行｜判断（大于）"></a><strong>30. 筛选行｜判断（大于）</strong></h3><ul><li><strong>题目</strong>: 提取 金牌数 大于 30 的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_gold_30 = df[df[<span class="hljs-string">&#x27;金牌数&#x27;</span>] &gt; <span class="hljs-number">30</span>]<br>df_gold_30<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>这是 Pandas 中最核心的<strong>布尔索引</strong>。<code>df[&#39;金牌数&#39;] &gt; 30</code> 会生成一个布尔值的 Series，用它来过滤 <code>df</code>，<code>df</code> 会只保留那些对应值为 <code>True</code> 的行。</li></ul></li></ul><h3 id="31-筛选行-｜判断（等于）"><a href="#31-筛选行-｜判断（等于）" class="headerlink" title="31. 筛选行 ｜判断（等于）"></a><strong>31. 筛选行 ｜判断（等于）</strong></h3><ul><li><strong>题目</strong>: 提取 <code>金牌数</code> 等于 10 的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_gold_10 = df[df[<span class="hljs-string">&#x27;金牌数&#x27;</span>] == <span class="hljs-number">10</span>]<br>df_gold_10<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>与上一题同理，只是比较运算符从 <code>&gt;</code> 变成了 <code>==</code>。</li></ul></li></ul><h3 id="32-筛选行｜判断（不等于）"><a href="#32-筛选行｜判断（不等于）" class="headerlink" title="32. 筛选行｜判断（不等于）"></a><strong>32. 筛选行｜判断（不等于）</strong></h3><ul><li><strong>题目</strong>: 提取 <code>金牌数</code> 不等于 <code>10</code> 的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_not_gold_10 = df[df[<span class="hljs-string">&#x27;金牌数&#x27;</span>] != <span class="hljs-number">10</span>]<br>df_not_gold_10.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>不等于的运算符是 <code>!=</code>。</li></ul></li></ul><h3 id="33-筛选行｜条件（指定行号）"><a href="#33-筛选行｜条件（指定行号）" class="headerlink" title="33. 筛选行｜条件（指定行号）"></a><strong>33. 筛选行｜条件（指定行号）</strong></h3><ul><li><strong>题目</strong>: 提取全部 <strong>奇数行</strong>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_odd_rows = df.iloc[<span class="hljs-number">1</span>::<span class="hljs-number">2</span>]<br>df_odd_rows.head()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>同样是利用 <code>iloc</code> 的切片步长功能。</li></ul></li></ul><h3 id="34-筛选行｜条件（指定值）"><a href="#34-筛选行｜条件（指定值）" class="headerlink" title="34. 筛选行｜条件（指定值）"></a><strong>34. 筛选行｜条件（指定值）</strong></h3><ul><li><strong>题目</strong>: 提取 中国、美国、英国 三行数据。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">countries = [<span class="hljs-string">&#x27;中华人民共和国&#x27;</span>, <span class="hljs-string">&#x27;美国&#x27;</span>, <span class="hljs-string">&#x27;英国&#x27;</span>]<br>df_selected_countries = df[df[<span class="hljs-string">&#x27;国家奥委会&#x27;</span>].isin(countries)]<br>df_selected_countries<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>isin()</code> 方法非常适合筛选“等于多个值中任意一个”的情况。它接收一个列表，返回一个布尔 Series。</li></ul></li></ul><h3 id="35-筛选行｜多条件"><a href="#35-筛选行｜多条件" class="headerlink" title="35. 筛选行｜多条件"></a><strong>35. 筛选行｜多条件</strong></h3><ul><li><strong>题目</strong>: 在上一题的条件下，新增一个条件：<strong>金牌数小于30</strong></li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">countries = [<span class="hljs-string">&#x27;中华人民共和国&#x27;</span>, <span class="hljs-string">&#x27;美国&#x27;</span>, <span class="hljs-string">&#x27;英国&#x27;</span>]<br>df_multi_cond = df[(df[<span class="hljs-string">&#x27;国家奥委会&#x27;</span>].isin(countries)) &amp; (df[<span class="hljs-string">&#x27;金牌数&#x27;</span>] &lt; <span class="hljs-number">30</span>)]<br>df_multi_cond<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>多个条件组合时，使用 <code>&amp;</code> 代表“与”(and)，<code>|</code> 代表“或”(or)。</li><li><strong>务必用括号 <code>()</code> 将每个独立的条件括起来</strong>，这是因为 Python 运算符优先级的原因。</li></ul></li></ul><h3 id="36-筛选行｜条件（包含指定值）"><a href="#36-筛选行｜条件（包含指定值）" class="headerlink" title="36. 筛选行｜条件（包含指定值）"></a><strong>36. 筛选行｜条件（包含指定值）</strong></h3><ul><li><strong>题目</strong>: 提取 <code>国家奥委会</code> 列中，所有包含 <code>国</code>的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_contains_guo = df[df[<span class="hljs-string">&#x27;国家奥委会&#x27;</span>].<span class="hljs-built_in">str</span>.contains(<span class="hljs-string">&#x27;国&#x27;</span>, na=<span class="hljs-literal">False</span>)]<br>df_contains_guo<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.str</code> 访问器允许我们对整列字符串进行操作。</li><li><code>.str.contains(&#39;国&#39;)</code> 会检查每个字符串是否包含“国”字。</li><li><code>na=False</code> 表示如果遇到缺失值，则直接返回 <code>False</code>，避免出错。</li></ul></li></ul><h3 id="37-筛选某行某列"><a href="#37-筛选某行某列" class="headerlink" title="37. 筛选某行某列"></a><strong>37. 筛选某行某列</strong></h3><ul><li><strong>题目</strong>: 提取 <code>第 0 行第 2 列</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># .iloc[行位置, 列位置]</span><br>value_0_2 = df.iloc[<span class="hljs-number">0</span>, <span class="hljs-number">2</span>]<br>value_0_2<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>iloc</code> 中用逗号分隔行和列的位置，可以直接定位到单个值。</li></ul></li></ul><h3 id="38-筛选多行多列"><a href="#38-筛选多行多列" class="headerlink" title="38. 筛选多行多列"></a><strong>38. 筛选多行多列</strong></h3><ul><li><strong>题目</strong>: 提取 第 0-2 行第 0-2 列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_block = df.iloc[<span class="hljs-number">0</span>:<span class="hljs-number">3</span>, <span class="hljs-number">0</span>:<span class="hljs-number">3</span>]<br>df_block<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>行和列都使用切片语法，可以方便地提取出一个矩形区块。</li></ul></li></ul><h3 id="39-筛选值｜组合（行号-列号）"><a href="#39-筛选值｜组合（行号-列号）" class="headerlink" title="39. 筛选值｜组合（行号+列号）"></a><strong>39. 筛选值｜组合（行号+列号）</strong></h3><ul><li><strong>题目</strong>: 提取第 4 行，第 4 列的值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">value_4_4 = df.iloc[<span class="hljs-number">4</span>, <span class="hljs-number">4</span>] <span class="hljs-comment"># 第5行是索引4，第5列是索引4</span><br>value_4_4<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>与37题同理，使用 <code>iloc</code> 进行精确定位。</li></ul></li></ul><h3 id="40-筛选值｜组合（行号-列名）"><a href="#40-筛选值｜组合（行号-列名）" class="headerlink" title="40. 筛选值｜组合（行号+列名）"></a><strong>40. 筛选值｜组合（行号+列名）</strong></h3><ul><li><strong>题目</strong>: 提取行索引为 4 ，列名为 金牌数 的值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">value_loc = df.loc[<span class="hljs-number">4</span>, <span class="hljs-string">&#x27;金牌数&#x27;</span>]<br>value_loc<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>loc</code> 用于基于<strong>标签</strong>的索引。这里行标签是 <code>4</code>，列标签是 <code>&#39;金牌数&#39;</code>。</li></ul></li></ul><h3 id="41-筛选值｜条件"><a href="#41-筛选值｜条件" class="headerlink" title="41. 筛选值｜条件"></a><strong>41. 筛选值｜条件</strong></h3><ul><li><strong>题目</strong>: 提取 国家奥委会 为 <code>中华人民共和国</code> 的金牌数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先筛选行，再选择列</span><br>china_gold = df.loc[df[<span class="hljs-string">&#x27;国家奥委会&#x27;</span>] == <span class="hljs-string">&#x27;中华人民共和国&#x27;</span>, <span class="hljs-string">&#x27;金牌数&#x27;</span>]<br><span class="hljs-comment"># 因为结果只有一个值，它会是一个Series，可以用.item()或.values[0]提取纯数值</span><br>china_gold.item()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>这是一种非常常见的组合用法：<code>df.loc[行过滤条件, &#39;目标列名&#39;]</code>。</li></ul></li></ul><h3 id="42-筛选值｜query"><a href="#42-筛选值｜query" class="headerlink" title="42. 筛选值｜query"></a><strong>42. 筛选值｜query</strong></h3><ul><li><strong>题目</strong>: 使用 <code>query</code> 提取 金牌数 + 银牌数 大于 70 的国家。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df_query_1 = df.query(<span class="hljs-string">&#x27;金牌数 + 银牌数 &gt; 70&#x27;</span>)<br>df_query_1<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>query()</code> 方法允许你使用字符串表达式来筛选数据，当条件复杂时，代码可读性比布尔索引更好。可以直接在字符串中书写列名和计算。</li></ul></li></ul><h3 id="43-筛选值｜query（引用变量）"><a href="#43-筛选值｜query（引用变量）" class="headerlink" title="43. 筛选值｜query（引用变量）"></a><strong>43. 筛选值｜query（引用变量）</strong></h3><ul><li><strong>题目</strong>: 使用 <code>query</code> 提取 金牌数 大于 金牌均值的国家。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先计算均值</span><br>gold_mean = df[<span class="hljs-string">&#x27;金牌数&#x27;</span>].mean()<br><br><span class="hljs-comment"># 在query字符串中使用@符号引用外部变量</span><br>df_query_2 = df.query(<span class="hljs-string">&#x27;金牌数 &gt; @gold_mean&#x27;</span>)<br>df_query_2<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>在 <code>query</code> 表达式字符串中，你可以在变量名前加上 <code>@</code> 符号，来引用在代码环境中已定义的外部变量。</li></ul></li></ul>]]></content>
      
      
      <categories>
          
          <category> pandas </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pandas </tag>
            
            <tag> 数据处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>4.数据统计描述性分析</title>
      <link href="/2025/4-%E6%95%B0%E6%8D%AE%E7%BB%9F%E8%AE%A1%E6%8F%8F%E8%BF%B0%E6%80%A7%E5%88%86%E6%9E%90/"/>
      <url>/2025/4-%E6%95%B0%E6%8D%AE%E7%BB%9F%E8%AE%A1%E6%8F%8F%E8%BF%B0%E6%80%A7%E5%88%86%E6%9E%90/</url>
      
        <content type="html"><![CDATA[<h3 id="初始化与加载数据"><a href="#初始化与加载数据" class="headerlink" title="初始化与加载数据"></a><strong>初始化与加载数据</strong></h3><p>首先，我们导入 <code>pandas</code> 库，并加载本期练习所需的数据集 <code>2020年中国大学排名.xlsx</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> pandas <span class="hljs-keyword">as</span> pd<br><br><span class="hljs-comment"># 加载数据</span><br>df = pd.read_excel(<span class="hljs-string">&quot;2020年中国大学排名.xlsx&quot;</span>)<br></code></pre></td></tr></table></figure><hr><h2 id="数据探索"><a href="#数据探索" class="headerlink" title="数据探索"></a><strong>数据探索</strong></h2><p>这部分将通过排序、统计和可视化等多种手段，对大学排名数据进行探索性分析。</p><h3 id="1-查看数据"><a href="#1-查看数据" class="headerlink" title="1. 查看数据"></a><strong>1. 查看数据</strong></h3><ul><li><strong>题目</strong>: 查看数据前 10 行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.head(<span class="hljs-number">10</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.head(n)</code> 用于显示 DataFrame 的前 n 行，这里 <code>n=10</code>。这能帮助我们快速预览数据的主体内容和列名，对数据建立初步印象。</li></ul></li></ul><h3 id="2-修改索引"><a href="#2-修改索引" class="headerlink" title="2. 修改索引"></a><strong>2. 修改索引</strong></h3><ul><li><strong>题目</strong>: 将索引修改为 学校名称 列。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.set_index(<span class="hljs-string">&#x27;学校名称&#x27;</span>, inplace=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>set_index()</code> 方法可以将 DataFrame 的某一列或多列设置为新的行索引。</li><li><code>inplace=True</code> 参数会<strong>直接在原始 DataFrame 上进行修改</strong>，而不会返回一个新的 DataFrame。这样做可以节省内存。修改后，我们就可以通过学校名称（如 <code>df.loc[&#39;清华大学&#39;]</code>）来直接访问数据行，更加直观。</li></ul></li></ul><h3 id="3-查看数据量"><a href="#3-查看数据量" class="headerlink" title="3. 查看数据量"></a><strong>3. 查看数据量</strong></h3><ul><li><strong>题目</strong>: 计算数据框的 行 * 列，总共单元格的数量。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.size<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.size</code> 属性返回 DataFrame 中元素的总数，即 <strong>行数 × 列数</strong>。这与返回 <code>(行数, 列数)</code> 元组的 <code>df.shape</code> 不同。<code>size</code> 告诉我们数据集中总共有多少个数据点。</li></ul></li></ul><h3 id="4-数据排序"><a href="#4-数据排序" class="headerlink" title="4. 数据排序"></a><strong>4. 数据排序</strong></h3><ul><li><strong>题目</strong>: 将数据按照总分升序排列，并展示前20个学校（即倒数20名）。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.sort_values(by=<span class="hljs-string">&#x27;总分&#x27;</span>, ascending=<span class="hljs-literal">True</span>).head(<span class="hljs-number">20</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>sort_values()</code> 是用于排序的核心方法。</li><li><code>by=&#39;总分&#39;</code> 指定了我们根据哪一列进行排序。</li><li><code>ascending=True</code> 表示<strong>升序</strong>排列。默认值为 <code>True</code>，所以当需要升序时也可以省略。</li><li><code>.head(20)</code> 链式调用，紧接着选取排序后结果的前 20 行。</li></ul></li></ul><h3 id="5-数据排序"><a href="#5-数据排序" class="headerlink" title="5. 数据排序"></a><strong>5. 数据排序</strong></h3><ul><li><strong>题目</strong>: 将数据按照 高端人才得分 降序排序，展示前 10 位。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.sort_values(by=<span class="hljs-string">&#x27;高端人才得分&#x27;</span>, ascending=<span class="hljs-literal">False</span>).head(<span class="hljs-number">10</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>与上一题类似，但 <code>ascending=False</code> 表示<strong>降序</strong>排列，即将分值高的排在前面。</li></ul></li></ul><h3 id="6-分列排名"><a href="#6-分列排名" class="headerlink" title="6. 分列排名"></a><strong>6. 分列排名</strong></h3><ul><li><strong>题目</strong>: 查看各项得分最高的学校名称。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 筛选出所有得分列</span><br>score_cols = df.columns[<span class="hljs-number">3</span>:]<br><span class="hljs-comment"># 使用 idxmax() 找到每列最大值对应的索引（即学校名称）</span><br>df[score_cols].idxmax()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>idxmax()</code> 是一个非常实用的函数，它会返回每列中<strong>最大值第一次出现的索引</strong>。因为我们已经将“学校名称”设置为了索引，所以结果直接就是学校的名字。这比先找到最大值再去找对应的学校要高效得多。</li></ul></li></ul><h3 id="7-统计信息｜均值"><a href="#7-统计信息｜均值" class="headerlink" title="7. 统计信息｜均值"></a><strong>7. 统计信息｜均值</strong></h3><ul><li><strong>题目</strong>: 计算总分列的均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;总分&#x27;</span>].mean()<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>.mean()</code> 方法用于计算 Series 或 DataFrame 列的算术平均值。</li></ul></li></ul><h3 id="8-统计信息｜中位数"><a href="#8-统计信息｜中位数" class="headerlink" title="8. 统计信息｜中位数"></a><strong>8. 统计信息｜中位数</strong></h3><ul><li><strong>题目</strong>: 计算总分列的中位数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;总分&#x27;</span>].median()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.median()</code> 方法用于计算中位数。中位数是将数据排序后位于中间位置的那个值，它比均值更能抵抗极端值（异常值）的影响。</li></ul></li></ul><h3 id="9-统计信息｜众数"><a href="#9-统计信息｜众数" class="headerlink" title="9. 统计信息｜众数"></a><strong>9. 统计信息｜众数</strong></h3><ul><li><strong>题目</strong>: 计算总分列的众数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;总分&#x27;</span>].mode()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.mode()</code> 方法用于计算众数，即数据集中出现次数最多的那个值。</li><li>注意它返回的是一个 <strong>Series</strong>，因为数据中可能存在多个众数（出现次数相同）。</li></ul></li></ul><h3 id="10-统计信息｜部分"><a href="#10-统计信息｜部分" class="headerlink" title="10. 统计信息｜部分"></a><strong>10. 统计信息｜部分</strong></h3><ul><li><strong>题目</strong>: 计算 总分、高端人才得分、办学层次得分的最大最小值、中位数、均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">cols = [<span class="hljs-string">&#x27;总分&#x27;</span>, <span class="hljs-string">&#x27;高端人才得分&#x27;</span>, <span class="hljs-string">&#x27;办学层次得分&#x27;</span>]<br>stats_to_calc = [<span class="hljs-string">&#x27;max&#x27;</span>, <span class="hljs-string">&#x27;min&#x27;</span>, <span class="hljs-string">&#x27;median&#x27;</span>, <span class="hljs-string">&#x27;mean&#x27;</span>]<br>df[cols].agg(stats_to_calc)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.agg()</code> (aggregate 的缩写) 是一个极其强大的函数，它允许你对指定的列同时应用<strong>一个或多个</strong>聚合函数。</li><li>我们只需要将目标列和想计算的统计函数名称列表传入即可，代码非常整洁。</li></ul></li></ul><h3 id="11-统计信息｜完整"><a href="#11-统计信息｜完整" class="headerlink" title="11. 统计信息｜完整"></a><strong>11. 统计信息｜完整</strong></h3><ul><li><strong>题目</strong>: 查看数值型数据的统计信息（均值、分位数等），并保留两位小数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.describe().<span class="hljs-built_in">round</span>(<span class="hljs-number">2</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.describe()</code> 我们在上一期已经学过，用于生成描述性统计摘要。</li><li><code>.round(2)</code> 方法可以链式调用，将结果中的所有数值四舍五入到指定的小数位数（这里是两位），使输出更美观、易读。</li></ul></li></ul><h3 id="12-统计信息｜分组"><a href="#12-统计信息｜分组" class="headerlink" title="12. 统计信息｜分组"></a><strong>12. 统计信息｜分组</strong></h3><ul><li><strong>题目</strong>: 计算各省市总分均值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.groupby(<span class="hljs-string">&#x27;省市&#x27;</span>)[<span class="hljs-string">&#x27;总分&#x27;</span>].mean()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>groupby()</code> 是 Pandas 的核心功能之一，用于实现“<strong>分割-应用-合并</strong>” (Split-Apply-Combine) 的操作。</li><li><code>df.groupby(&#39;省市&#39;)</code>: 首先根据“省市”列将数据分割成不同的组。</li><li><code>[&#39;总分&#39;]</code>: 然后在每个组内，选取“总分”这一列。</li><li><code>.mean()</code>: 最后对每个组的“总分”计算均值，并将结果合并成一个新的 Series。</li></ul></li></ul><h3 id="13-统计信息｜相关系数"><a href="#13-统计信息｜相关系数" class="headerlink" title="13. 统计信息｜相关系数"></a><strong>13. 统计信息｜相关系数</strong></h3><ul><li><strong>题目</strong>: 计算相关系数矩阵。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.corr()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.corr()</code> 方法用于计算 DataFrame 中所有<strong>数值列</strong>两两之间的<strong>皮尔逊相关系数</strong>。</li><li>结果是一个矩阵，对角线上的值恒为 1（自身与自身相关）。矩阵中的值范围在 -1 到 1 之间，表示两列数据的线性相关程度：<ul><li>接近 1：强正相关（一列增加，另一列也倾向于增加）</li><li>接近 -1：强负相关（一列增加，另一列倾向于减少）</li><li>接近 0：无线性相关</li></ul></li></ul></li></ul><h3 id="14-相关系数｜热力图"><a href="#14-相关系数｜热力图" class="headerlink" title="14. 相关系数｜热力图"></a><strong>14. 相关系数｜热力图</strong></h3><ul><li><strong>题目</strong>: 将上一题的相关性系数矩阵制作为热力图。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 需要先导入可视化库</span><br><span class="hljs-keyword">import</span> seaborn <span class="hljs-keyword">as</span> sns<br><span class="hljs-keyword">import</span> matplotlib.pyplot <span class="hljs-keyword">as</span> plt<br><br><span class="hljs-comment"># 计算相关系数</span><br>correlation_matrix = df.corr()<br><br><span class="hljs-comment"># 绘制热力图</span><br>plt.figure(figsize=(<span class="hljs-number">10</span>, <span class="hljs-number">8</span>)) <span class="hljs-comment"># 设置画布大小</span><br>sns.heatmap(correlation_matrix, annot=<span class="hljs-literal">True</span>, cmap=<span class="hljs-string">&#x27;coolwarm&#x27;</span>)<br>plt.show()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>热力图是可视化相关系数矩阵的最佳方式。</li><li><code>seaborn.heatmap()</code>:<ul><li>第一个参数是数据，即我们的相关系数矩阵。</li><li><code>annot=True</code>: 在每个格子上显示数值。</li><li><code>cmap=&#39;coolwarm&#39;</code>: 指定颜色映射方案，正相关（红色）和负相关（蓝色）一目了然。</li></ul></li></ul></li></ul><h3 id="15-统计信息｜频率"><a href="#15-统计信息｜频率" class="headerlink" title="15. 统计信息｜频率"></a><strong>15. 统计信息｜频率</strong></h3><ul><li><strong>题目</strong>: 计算各省市出现的次数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;省市&#x27;</span>].value_counts()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>.value_counts()</code> 是一个非常便捷的方法，用于统计一个 Series 中每个唯一值出现的次数，并按频率降序排列。</li></ul></li></ul><h3 id="16-统计信息｜热力地图"><a href="#16-统计信息｜热力地图" class="headerlink" title="16. 统计信息｜热力地图"></a><strong>16. 统计信息｜热力地图</strong></h3><ul><li><strong>题目</strong>: 结合 <code>pyecharts</code> 将各省市高校上榜数量进行地图可视化。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 确保已安装 pyecharts: pip install pyecharts</span><br><span class="hljs-keyword">from</span> pyecharts <span class="hljs-keyword">import</span> options <span class="hljs-keyword">as</span> opts<br><span class="hljs-keyword">from</span> pyecharts.charts <span class="hljs-keyword">import</span> Map<br><br><span class="hljs-comment"># 1. 获取数据</span><br>province_counts = df[<span class="hljs-string">&#x27;省市&#x27;</span>].value_counts()<br><br><span class="hljs-comment"># 2. 准备 pyecharts 需要的数据格式：[(&#x27;省份&#x27;, 数量), ...]</span><br>data_list = <span class="hljs-built_in">list</span>(<span class="hljs-built_in">zip</span>(province_counts.index, province_counts.values.astype(<span class="hljs-built_in">str</span>)))<br><br><span class="hljs-comment"># 3. 创建地图</span><br>c = (<br>    Map()<br>    .add(<span class="hljs-string">&quot;高校上榜数量&quot;</span>, data_list, <span class="hljs-string">&quot;china&quot;</span>)<br>    .set_global_opts(<br>        title_opts=opts.TitleOpts(title=<span class="hljs-string">&quot;各省市TOP500高校上榜数量&quot;</span>),<br>        visualmap_opts=opts.VisualMapOpts(max_=<span class="hljs-built_in">int</span>(province_counts.<span class="hljs-built_in">max</span>())),<br>    )<br>)<br><span class="hljs-comment"># 在 Jupyter Notebook 中可以直接显示</span><br>c.render_notebook()<br><span class="hljs-comment"># 或者保存为 html 文件</span><br><span class="hljs-comment"># c.render(&quot;university_distribution_map.html&quot;)</span><br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>Pyecharts 是一个用于生成交互式图表的强大库。</li><li>这段代码首先获取省市的计数值，然后将其转换为 Pyecharts <code>Map</code> 图表需要的 <code>[(key, value), ...]</code> 格式。</li><li><code>.add()</code> 方法将数据添加到地图上。</li><li><code>.set_global_opts()</code> 用于设置图表的全局选项，如图表标题、视觉映射范围等。</li></ul></li></ul><h3 id="17-统计信息｜直方图"><a href="#17-统计信息｜直方图" class="headerlink" title="17. 统计信息｜直方图"></a><strong>17. 统计信息｜直方图</strong></h3><ul><li><strong>题目</strong>: 绘制总分的直方图、密度估计图。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> seaborn <span class="hljs-keyword">as</span> sns<br><span class="hljs-keyword">import</span> matplotlib.pyplot <span class="hljs-keyword">as</span> plt<br><br>sns.histplot(df[<span class="hljs-string">&#x27;总分&#x27;</span>], kde=<span class="hljs-literal">True</span>)<br>plt.title(<span class="hljs-string">&#x27;总分分布直方图&#x27;</span>)<br>plt.show()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>直方图是观察单个数值变量分布的绝佳工具。</li><li><code>seaborn.histplot()</code>:<ul><li>第一个参数是要绘制的数据列。</li><li><code>kde=True</code>: (Kernel Density Estimate) 会在直方图上叠加一条平滑的<strong>核密度估计曲线</strong>，更清晰地展示数据分布的形状。</li></ul></li></ul></li></ul><hr><h2 id="2-个-pandas-EDA-插件"><a href="#2-个-pandas-EDA-插件" class="headerlink" title="2 个 pandas EDA 插件"></a><strong>2 个 pandas EDA 插件</strong></h2><p>除了手动一步步分析，社区也开发了一些强大的工具，可以一键生成详细的探索性数据分析（EDA）报告。</p><h3 id="18-pandas-profiling"><a href="#18-pandas-profiling" class="headerlink" title="18. pandas_profiling"></a><strong>18. pandas_profiling</strong></h3><ul><li><strong>解释</strong>:<ul><li><code>pandas-profiling</code> (现在叫 <code>ydata-profiling</code>) 是一个能自动生成交互式 EDA 报告的库。报告内容极其详尽，包括每列的统计信息、分布图、相关性、缺失值分析等等。</li></ul></li><li><strong>代码</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 如果未安装，先执行: !pip install ydata-profiling</span><br><span class="hljs-keyword">from</span> ydata_profiling <span class="hljs-keyword">import</span> ProfileReport<br><br>profile = ProfileReport(df, title=<span class="hljs-string">&quot;大学排名数据分析报告&quot;</span>)<br>profile <span class="hljs-comment"># 在 Jupyter Notebook 中，这行代码会自动渲染报告</span><br></code></pre></td></tr></table></figure></li></ul><h3 id="19-sweetviz"><a href="#19-sweetviz" class="headerlink" title="19. sweetviz"></a><strong>19. sweetviz</strong></h3><ul><li><strong>解释</strong>:<ul><li><code>sweetviz</code> 是另一个自动化 EDA 工具，它的特点是能很方便地对两个数据集（例如训练集和测试集）进行对比分析。它生成的报告同样是交互式的 HTML 文件。</li></ul></li><li><strong>代码</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 如果未安装，先执行: !pip install sweetviz</span><br><span class="hljs-keyword">import</span> sweetviz <span class="hljs-keyword">as</span> sv<br><br><span class="hljs-comment"># 分析数据集</span><br>report = sv.analyze(df)<br><br><span class="hljs-comment"># 在浏览器中显示报告</span><br>report.show_html(<span class="hljs-string">&#x27;sweetviz_report.html&#x27;</span>)<br></code></pre></td></tr></table></figure></li></ul>]]></content>
      
      
      <categories>
          
          <category> pandas </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pandas </tag>
            
            <tag> 数据处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>3.数据预览与预处理</title>
      <link href="/2025/3-%E6%95%B0%E6%8D%AE%E9%A2%84%E8%A7%88%E4%B8%8E%E9%A2%84%E5%A4%84%E7%90%86/"/>
      <url>/2025/3-%E6%95%B0%E6%8D%AE%E9%A2%84%E8%A7%88%E4%B8%8E%E9%A2%84%E5%A4%84%E7%90%86/</url>
      
        <content type="html"><![CDATA[<h3 id="初始化与加载数据"><a href="#初始化与加载数据" class="headerlink" title="初始化与加载数据"></a><strong>初始化与加载数据</strong></h3><p>在开始之前，我们还是需要先导入 <code>pandas</code> 库，并加载本期练习所需的数据集 <code>TOP250.xlsx</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> pandas <span class="hljs-keyword">as</span> pd<br><br><span class="hljs-comment"># 加载数据</span><br>df = pd.read_excel(<span class="hljs-string">&quot;TOP250.xlsx&quot;</span>)<br></code></pre></td></tr></table></figure><hr><h2 id="数据查看"><a href="#数据查看" class="headerlink" title="数据查看"></a><strong>数据查看</strong></h2><p>拿到一个数据集后，首要任务就是像侦探一样，从不同角度审视它，了解其规模、结构和内容。</p><h3 id="1-查看数据维度"><a href="#1-查看数据维度" class="headerlink" title="1. 查看数据维度"></a><strong>1. 查看数据维度</strong></h3><ul><li><strong>题目</strong>: 先看看数据多少行，多少列，对接下来的处理量心里有个数。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.shape<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.shape</code> 是 DataFrame 的一个属性（注意后面没有括号），它会返回一个**元组 (tuple)**。</li><li>元组的第一个元素代表<strong>行数</strong>，第二个元素代表<strong>列数</strong>。</li><li>这是了解数据规模最快、最直接的方法。</li></ul></li></ul><h3 id="2-随机查看5条数据"><a href="#2-随机查看5条数据" class="headerlink" title="2. 随机查看5条数据"></a><strong>2. 随机查看5条数据</strong></h3><ul><li><strong>题目</strong>: 随机查看5条数据。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.sample(<span class="hljs-number">5</span>)<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>df.sample(n)</code> 是一个非常有用的方法，它可以从 DataFrame 中<strong>随机抽取 n 条</strong>数据。</li><li>为什么要用随机查看？相比于只看开头或结尾的数据，随机抽样能更客观地反映数据集中间部分的情况，避免因数据排序带来的片面印象。</li></ul></li></ul><h3 id="3-查看数据前后5行"><a href="#3-查看数据前后5行" class="headerlink" title="3. 查看数据前后5行"></a><strong>3. 查看数据前后5行</strong></h3><ul><li><strong>题目</strong>: 查看数据前后5行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 查看前5行</span><br>df.head()<br><br><span class="hljs-comment"># 查看后5行</span><br>df.tail()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.head(n)</code> 查看 DataFrame 的前 n 行（默认为 5）。</li><li><code>df.tail(n)</code> 查看 DataFrame 的后 n 行（默认为 5）。</li><li>这两个方法可以帮助我们快速检查数据是否加载正确，表头是什么，数据的大致格式，以及是否有明显的排序规律。</li></ul></li></ul><h3 id="4-查看数据基本信息"><a href="#4-查看数据基本信息" class="headerlink" title="4. 查看数据基本信息"></a><strong>4. 查看数据基本信息</strong></h3><ul><li><strong>题目</strong>: 看看数据类型，有无缺失值什么的。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.info()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.info()</code> 是数据概览的“神器”，它会打印出 DataFrame 的摘要信息，包括：<ul><li><strong>索引类型和范围</strong>: <code>RangeIndex: 250 entries, 0 to 249</code></li><li><strong>总列数</strong>: <code>Data columns (total 10 columns):</code></li><li><strong>每列的信息</strong>:<ul><li><strong>列名</strong>: 如 <code>片名</code></li><li><strong>非缺失值数量</strong>: <code>248 non-null</code>，这直接告诉我们该列有多少缺失值（总行数 - 非缺失值数量）。</li><li><strong>数据类型 (Dtype)</strong>: 如 <code>object</code> (通常是字符串)、<code>float64</code> (浮点数)、<code>int64</code> (整数)。</li></ul></li><li><strong>内存占用</strong>: <code>memory usage: ...</code></li></ul></li></ul></li></ul><h3 id="5-查看数据统计信息｜数值"><a href="#5-查看数据统计信息｜数值" class="headerlink" title="5. 查看数据统计信息｜数值"></a><strong>5. 查看数据统计信息｜数值</strong></h3><ul><li><strong>题目</strong>: 查看 <strong>数值型</strong> 列的统计信息，计数、均值什么的。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.describe()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.describe()</code> 会对所有<strong>数值类型</strong>（如 <code>int64</code>, <code>float64</code>）的列进行描述性统计，输出包括：<ul><li><code>count</code>: 非缺失值的数量</li><li><code>mean</code>: 平均值</li><li><code>std</code>: 标准差</li><li><code>min</code>: 最小值</li><li><code>25%</code>, <code>50%</code>, <code>75%</code>: 分别代表第一、第二（中位数）、第三四分位数</li><li><code>max</code>: 最大值</li></ul></li><li>这能帮我们快速了解数值数据的分布情况，比如评分范围、评价人数的量级等。</li></ul></li></ul><h3 id="6-查看数据统计信息｜离散"><a href="#6-查看数据统计信息｜离散" class="headerlink" title="6. 查看数据统计信息｜离散"></a><strong>6. 查看数据统计信息｜离散</strong></h3><ul><li><strong>题目</strong>: 查看 <strong>离散型</strong> 列的统计信息，计数、频率什么。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.describe(include=[<span class="hljs-string">&#x27;object&#x27;</span>])<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>describe()</code> 方法可以通过 <code>include</code> 参数来指定要统计的数据类型。</li><li><code>include=[&#39;object&#39;]</code> 表示我们只想看<strong>对象类型（通常是字符串）</strong>列的统计信息，输出包括：<ul><li><code>count</code>: 非缺失值的数量</li><li><code>unique</code>: 有多少个不重复的类别</li><li><code>top</code>: 出现次数最多的类别是什么</li><li><code>freq</code>: <code>top</code> 类别出现的次数</li></ul></li></ul></li></ul><h3 id="7-查看数据统计信息｜整体"><a href="#7-查看数据统计信息｜整体" class="headerlink" title="7. 查看数据统计信息｜整体"></a><strong>7. 查看数据统计信息｜整体</strong></h3><ul><li><strong>题目</strong>: 查看 <strong>全部</strong> 列的统计信息。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.describe(include=<span class="hljs-string">&#x27;all&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>include=&#39;all&#39;</code> 会将数值型和离散型列的统计信息合并在一起展示。</li><li>对于数值列，离散型统计（如 <code>unique</code>, <code>top</code>）会显示为 <code>NaN</code>；反之亦然。</li></ul></li></ul><hr><h2 id="缺失值处理"><a href="#缺失值处理" class="headerlink" title="缺失值处理"></a><strong>缺失值处理</strong></h2><p>数据很少是完美的，缺失值是家常便饭。处理缺失值是数据预处理的核心步骤。</p><h3 id="8-计算缺失值｜总计"><a href="#8-计算缺失值｜总计" class="headerlink" title="8. 计算缺失值｜总计"></a><strong>8. 计算缺失值｜总计</strong></h3><ul><li><strong>题目</strong>: 看看一共存在多少个缺失值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.isnull().<span class="hljs-built_in">sum</span>().<span class="hljs-built_in">sum</span>()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.isnull()</code>: 这个操作会返回一个和 <code>df</code> 同样大小的 DataFrame，但元素是布尔值 (<code>True</code>&#x2F;<code>False</code>)。<code>True</code> 表示该位置是缺失值 (<code>NaN</code>)，<code>False</code> 则表示不是。</li><li><code>df.isnull().sum()</code>: 对布尔值 DataFrame 按列求和。<code>True</code> 被当作 1，<code>False</code> 被当作 0。所以结果是一个 Series，显示<strong>每列</strong>的缺失值数量。</li><li><code>df.isnull().sum().sum()</code>: 对上一步得到的 Series 再次求和，得到整个 DataFrame 的<strong>总缺失值数量</strong>。</li></ul></li></ul><h3 id="9-计算缺失值｜分列"><a href="#9-计算缺失值｜分列" class="headerlink" title="9. 计算缺失值｜分列"></a><strong>9. 计算缺失值｜分列</strong></h3><ul><li><strong>题目</strong>: 再看看具体每列有多少缺失值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.isnull().<span class="hljs-built_in">sum</span>()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>这就是上一步的中间结果，它清晰地列出了每一列包含的缺失值个数，让我们能定位到问题所在的列。</li></ul></li></ul><h3 id="10-查看缺失值"><a href="#10-查看缺失值" class="headerlink" title="10. 查看缺失值"></a><strong>10. 查看缺失值</strong></h3><ul><li><strong>题目</strong>: 看看全部缺失值所在的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[df.isnull().<span class="hljs-built_in">any</span>(axis=<span class="hljs-number">1</span>)]<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.isnull()</code>: 得到布尔值 DataFrame。</li><li><code>.any(axis=1)</code>: <code>any()</code> 方法用于检查是否有任何 <code>True</code> 存在。<code>axis=1</code> 表示<strong>按行检查</strong>。所以，如果某一行有任意一个单元格是 <code>True</code>（即有缺失值），<code>any()</code> 就会返回 <code>True</code>。</li><li><code>df[...]</code>: 最后，我们用这个布尔值的 Series 来过滤原始的 DataFrame，就只筛选出了那些至少包含一个缺失值的行。</li></ul></li></ul><h3 id="11-高亮缺失值"><a href="#11-高亮缺失值" class="headerlink" title="11. 高亮缺失值"></a><strong>11. 高亮缺失值</strong></h3><ul><li><strong>题目</strong>: 将缺失值进行高亮进一步查看。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df.style.highlight_null(color=<span class="hljs-string">&#x27;red&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.style</code> 会返回一个 Styler 对象，允许我们对 DataFrame 的显示样式进行美化（这在 Jupyter Notebook 中特别有用）。</li><li><code>.highlight_null()</code> 是一个样式函数，它会自动找到所有的缺失值，并用指定的颜色（这里是红色）进行高亮。这比纯文本查看要直观得多。</li></ul></li></ul><h3 id="12-删除缺失值"><a href="#12-删除缺失值" class="headerlink" title="12. 删除缺失值"></a><strong>12. 删除缺失值</strong></h3><ul><li><strong>题目</strong>: 将缺失值出现的行全部删掉。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_no_na = df.dropna()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>dropna()</code> 是处理缺失值最简单粗暴的方法。</li><li>默认情况下（<code>how=&#39;any&#39;</code>），它会删除任何<strong>包含至少一个缺失值</strong>的行。</li><li><strong>注意</strong>: 这是一种有损操作，可能会丢失大量信息，尤其是在数据稀疏的情况下，所以使用前要慎重。</li></ul></li></ul><h3 id="13-缺失值补全｜整体填充"><a href="#13-缺失值补全｜整体填充" class="headerlink" title="13. 缺失值补全｜整体填充"></a><strong>13. 缺失值补全｜整体填充</strong></h3><ul><li><strong>题目</strong>: 将全部缺失值替换为 <code>*</code>。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_filled_star = df.fillna(<span class="hljs-string">&#x27;*&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>fillna()</code> 是用于填充缺失值的核心方法。</li><li>当给它传入一个具体的值（如字符串 <code>*</code>、数字 0 等）时，它会将 DataFrame 中所有的 <code>NaN</code> 都替换成这个值。</li></ul></li></ul><h3 id="14-缺失值补全｜向上填充"><a href="#14-缺失值补全｜向上填充" class="headerlink" title="14. 缺失值补全｜向上填充"></a><strong>14. 缺失值补全｜向上填充</strong></h3><ul><li><strong>题目</strong>: 将评分列的缺失值，替换为上一个电影的评分。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;评分&#x27;</span>] = df[<span class="hljs-string">&#x27;评分&#x27;</span>].ffill()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>ffill()</code> (forward fill) 会用<strong>前一个非缺失值</strong>来填充当前的缺失值。</li><li>因为数据是按评分排序的，这种方法在一定程度上是合理的。与之对应的是 <code>bfill()</code> (backward fill)，用后一个值来填充。</li></ul></li></ul><h3 id="15-缺失值补全｜整体均值填充"><a href="#15-缺失值补全｜整体均值填充" class="headerlink" title="15. 缺失值补全｜整体均值填充"></a><strong>15. 缺失值补全｜整体均值填充</strong></h3><ul><li><strong>题目</strong>: 将评价人数列的缺失值，用整列的均值进行填充。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">mean_value = df[<span class="hljs-string">&#x27;评价人数&#x27;</span>].mean()<br>df[<span class="hljs-string">&#x27;评价人数&#x27;</span>] = df[<span class="hljs-string">&#x27;评价人数&#x27;</span>].fillna(mean_value)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li>这是处理数值型特征缺失值最常用的方法之一。</li><li><code>df[&#39;评价人数&#39;].mean()</code> 计算出该列的平均值。</li><li>然后将这个计算出的平均值作为 <code>fillna()</code> 的参数，对该列的缺失值进行填充。</li></ul></li></ul><h3 id="16-缺失值补全｜上下均值填充"><a href="#16-缺失值补全｜上下均值填充" class="headerlink" title="16. 缺失值补全｜上下均值填充"></a><strong>16. 缺失值补全｜上下均值填充</strong></h3><ul><li><strong>题目</strong>: 将评价人数列的缺失值，用上下数字的均值进行填充。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;评价人数&#x27;</span>] = df[<span class="hljs-string">&#x27;评价人数&#x27;</span>].interpolate()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>interpolate()</code> 是一个更高级的填充方法，专门用于数值数据。</li><li>默认情况下，它使用<strong>线性插值</strong>。简单来说，如果一个值缺失了，它会取其“邻居”（上一个和下一个有效值）的平均值来填充。这通常比用整列的均值填充更精确。</li></ul></li></ul><h3 id="17-缺失值补全｜匹配填充"><a href="#17-缺失值补全｜匹配填充" class="headerlink" title="17. 缺失值补全｜匹配填充"></a><strong>17. 缺失值补全｜匹配填充</strong></h3><ul><li><p><strong>题目</strong>: 填充 “语言” 列的缺失值，要求根据 “国家&#x2F;地区” 列的值进行填充。</p></li><li><p><strong>答案</strong>: (这里提供两种思路)</p><p><strong>方法一：使用 <code>groupby</code> 和 <code>transform</code> (更强大灵活)</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[<span class="hljs-string">&#x27;语言&#x27;</span>] = df.groupby(<span class="hljs-string">&#x27;国家/地区&#x27;</span>)[<span class="hljs-string">&#x27;语言&#x27;</span>].transform(<span class="hljs-keyword">lambda</span> x: x.mode()[<span class="hljs-number">0</span>] <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> x.mode().empty <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><p><strong>方法二：创建映射字典后填充 (更易于理解)</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 1. 创建一个“国家/地区”到“最常见语言”的映射</span><br>lang_map = df.groupby(<span class="hljs-string">&#x27;国家/地区&#x27;</span>)[<span class="hljs-string">&#x27;语言&#x27;</span>].apply(<span class="hljs-keyword">lambda</span> x: x.mode()[<span class="hljs-number">0</span>] <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> x.mode().empty <span class="hljs-keyword">else</span> <span class="hljs-literal">None</span>)<br><br><span class="hljs-comment"># 2. 填充缺失值</span><br>df[<span class="hljs-string">&#x27;语言&#x27;</span>] = df[<span class="hljs-string">&#x27;语言&#x27;</span>].fillna(df[<span class="hljs-string">&#x27;国家/地区&#x27;</span>].<span class="hljs-built_in">map</span>(lang_map))<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>这是一个非常经典的分组填充问题，核心思想是：用<strong>同一组</strong>内的数据特征来填充缺失值。</li><li><code>groupby(&#39;国家/地区&#39;)</code>: 首先按“国家&#x2F;地区”列进行分组。</li><li><code>[&#39;语言&#39;]</code>: 在每个分组内，我们关注“语言”这一列。</li><li><code>x.mode()[0]</code>: 对于每个国家分组内的所有语言 (<code>x</code>)，我们计算**众数 (<code>mode</code>)**，也就是出现次数最多的语言，并取第一个结果 (<code>[0]</code>)。</li><li><strong><code>transform</code></strong>: 它的强大之处在于，它会将计算结果（每个分组的众数）“广播”回原始的 DataFrame 形状，可以直接用于赋值。</li><li>第二种方法思路更清晰，先创建一个映射关系，再用 <code>.map()</code> 方法根据“国家&#x2F;地区”列的值去查找对应的语言来填充，两者效果一致。</li></ul></li></ul><hr><h2 id="重复值处理"><a href="#重复值处理" class="headerlink" title="重复值处理"></a><strong>重复值处理</strong></h2><p>重复的数据行会影响统计分析的准确性，需要识别并处理。</p><h3 id="18-查找重复值"><a href="#18-查找重复值" class="headerlink" title="18. 查找重复值"></a><strong>18. 查找重复值</strong></h3><ul><li><strong>题目</strong>: 将全部重复值所在的行筛选出来。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[df.duplicated()]<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>df.duplicated()</code> 会返回一个布尔 Series。对于某一行，如果它与<strong>前面出现过的某一行</strong>完全相同，则标记为 <code>True</code>。</li><li>用这个布尔 Series 过滤 DataFrame，即可找出所有（除第一次出现外的）重复行。</li></ul></li></ul><h3 id="19-查找重复值｜指定"><a href="#19-查找重复值｜指定" class="headerlink" title="19. 查找重复值｜指定"></a><strong>19. 查找重复值｜指定</strong></h3><ul><li><strong>题目</strong>: 查找 <code>片名</code> 列全部重复的行。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df[df.duplicated(subset=[<span class="hljs-string">&#x27;片名&#x27;</span>], keep=<span class="hljs-literal">False</span>)]<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>subset=[&#39;片名&#39;]</code>: <code>duplicated</code> 方法的 <code>subset</code> 参数让我们只根据指定的列（这里是 <code>片名</code>）来判断是否重复，而不是比较整行。</li><li><code>keep=False</code>: 这是一个关键参数。默认的 <code>keep=&#39;first&#39;</code> 只会标记第二次、第三次出现的重复项为 <code>True</code>。而 <code>keep=False</code> 会将<strong>所有</strong>出现重复的项（包括第一次出现的）都标记为 <code>True</code>。这能帮助我们看到所有相关的重复记录。</li></ul></li></ul><h3 id="20-删除重复值"><a href="#20-删除重复值" class="headerlink" title="20. 删除重复值"></a><strong>20. 删除重复值</strong></h3><ul><li><strong>题目</strong>: 删除全部的重复值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_deduplicated = df.drop_duplicates()<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>drop_duplicates()</code> 是用于删除重复行的方法。</li><li>默认情况下（<code>keep=&#39;first&#39;</code>），它会删除所有重复的行，只<strong>保留第一次出现</strong>的那一行。</li></ul></li></ul><h3 id="21-删除重复值｜指定"><a href="#21-删除重复值｜指定" class="headerlink" title="21. 删除重复值｜指定"></a><strong>21. 删除重复值｜指定</strong></h3><ul><li><strong>题目</strong>: 删除全部的重复值，但保留最后一次出现的值。</li><li><strong>答案</strong>:<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_deduplicated_last = df.drop_duplicates(keep=<span class="hljs-string">&#x27;last&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><strong>解释</strong>:<ul><li><code>keep=&#39;last&#39;</code> 参数会改变删除逻辑，删除所有重复的行，但<strong>保留最后一次出现</strong>的那一行。</li></ul></li></ul>]]></content>
      
      
      <categories>
          
          <category> pandas </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pandas </tag>
            
            <tag> 数据处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>1.pandas读取</title>
      <link href="/2025/1-pandas%E8%AF%BB%E5%8F%96/"/>
      <url>/2025/1-pandas%E8%AF%BB%E5%8F%96/</url>
      
        <content type="html"><![CDATA[<h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a><strong>初始化</strong></h3><p>在开始之前，我们需要先导入 <code>pandas</code> 库，这在数据分析中是约定俗成的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> pandas <span class="hljs-keyword">as</span> pd<br></code></pre></td></tr></table></figure><hr><h2 id="1-1-数据读取"><a href="#1-1-数据读取" class="headerlink" title="1-1 数据读取"></a><strong>1-1 数据读取</strong></h2><p>这部分主要练习如何使用 <code>pd.read_csv()</code> 和 <code>pd.read_excel()</code> 函数，以及它们丰富的参数来满足各种读取需求。</p><h3 id="1-读取-Excel-文件"><a href="#1-读取-Excel-文件" class="headerlink" title="1. 读取 Excel 文件"></a><strong>1. 读取 Excel 文件</strong></h3><ul><li><p><strong>题目</strong>: 读取当前目录下的 <code>某招聘网站数据.csv</code> 和 <code>TOP250.xlsx</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 读取 a.csv 文件</span><br>df_csv = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>)<br><br><span class="hljs-comment"># 读取 .xlsx 文件</span><br>df_excel = pd.read_excel(<span class="hljs-string">&quot;TOP250.xlsx&quot;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_csv()</code> 是专门用来读取 CSV (逗号分隔值) 文件的函数。CSV 是一种通用的、简单的文本文件格式。</li><li><code>pd.read_excel()</code> 用于读取 Excel 文件（<code>.xls</code> 或 <code>.xlsx</code> 格式）。</li><li>当文件与你的代码（或 Notebook）在同一个文件夹时，可以直接在函数中传入文件名。</li></ul></li></ul><h3 id="2-读取-Excel-文件｜指定位置"><a href="#2-读取-Excel-文件｜指定位置" class="headerlink" title="2. 读取 Excel 文件｜指定位置"></a><strong>2. 读取 Excel 文件｜指定位置</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>2 - 个性化显示设置/data.csv</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df = pd.read_csv(<span class="hljs-string">&quot;2 - 个性化显示设置/data.csv&quot;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:<br>当文件不在当前目录时，需要提供文件的<strong>相对路径</strong>或<strong>绝对路径</strong>。</p><ul><li><strong>相对路径</strong>: <code>2 - 个性化显示设置/data.csv</code> 就是一个相对路径，表示从当前位置出发，进入名为 <code>2 - 个性化显示设置</code> 的文件夹，再找到 <code>data.csv</code> 文件。</li><li><strong>绝对路径</strong>: 指的是文件在电脑中的完整路径，例如 <code>C:/Users/YourUser/Documents/pandas练习/2 - 个性化显示设置/data.csv</code>。</li></ul></li></ul><h3 id="3-读取-Excel-文件｜指定行（顺序）"><a href="#3-读取-Excel-文件｜指定行（顺序）" class="headerlink" title="3. 读取 Excel 文件｜指定行（顺序）"></a><strong>3. 读取 Excel 文件｜指定行（顺序）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件的前 20 行。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_first20 = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, nrows=<span class="hljs-number">20</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>nrows</code> 参数用于指定只读取文件的前 n 行。这在处理超大文件，想先快速查看其结构时非常有用。</li></ul></li></ul><h3 id="4-读取-Excel-文件｜指定行（跳过）"><a href="#4-读取-Excel-文件｜指定行（跳过）" class="headerlink" title="4. 读取 Excel 文件｜指定行（跳过）"></a><strong>4. 读取 Excel 文件｜指定行（跳过）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件并跳过前 20 行。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_skip20 = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, skiprows=<span class="hljs-number">20</span>)<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>skiprows</code> 参数用于在读取文件时跳过指定的行数。注意，这里是跳过文件开头的 20 行数据，但<strong>标题行</strong>也会被计算在内。如果想保留标题行，可以这样写：<code>skiprows=range(1, 21)</code>，表示跳过第 1 到 20 行（行号从0开始）。</li></ul></li></ul><h3 id="5-读取-Excel-文件｜指定行（条件）"><a href="#5-读取-Excel-文件｜指定行（条件）" class="headerlink" title="5. 读取 Excel 文件｜指定行（条件）"></a><strong>5. 读取 Excel 文件｜指定行（条件）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件中全部偶数行。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_even_rows = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, skiprows=<span class="hljs-keyword">lambda</span> x: x % <span class="hljs-number">2</span> != <span class="hljs-number">0</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>skiprows</code> 参数非常强大，它不仅能接收一个整数，还可以接收一个<strong>lambda 函数</strong>。</li><li><code>lambda x: x % 2 != 0</code> 的意思是：对于每一行（行号为 <code>x</code>），都进行判断。如果行号 <code>x</code> 除以 2 的余数不等于 0（即奇数行），则 <code>skiprows</code> 返回 <code>True</code>，这一行就会被跳过。所以最终只留下了偶数行。</li><li><strong>思考</strong>: 读取奇数行只需将条件改为 <code>lambda x: x &gt; 0 and x % 2 == 0</code> ( <code>x&gt;0</code> 是为了保留标题行)。</li></ul></li></ul><h3 id="6-读取-Excel-文件｜指定列（列号）"><a href="#6-读取-Excel-文件｜指定列（列号）" class="headerlink" title="6. 读取 Excel 文件｜指定列（列号）"></a><strong>6. 读取 Excel 文件｜指定列（列号）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件的第 1、3、5 列。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_cols_by_index = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, usecols=[<span class="hljs-number">0</span>, <span class="hljs-number">2</span>, <span class="hljs-number">4</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>usecols</code> 参数用于指定需要读取的列。</li><li>当传入一个<strong>列表</strong>时，可以根据<strong>列的索引号</strong>来选择。注意，索引号从 0 开始，所以第 1、3、5 列对应的索引是 0、2、4。</li></ul></li></ul><h3 id="7-读取-Excel-文件｜指定列（列名）"><a href="#7-读取-Excel-文件｜指定列（列名）" class="headerlink" title="7. 读取 Excel 文件｜指定列（列名）"></a><strong>7. 读取 Excel 文件｜指定列（列名）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件的 <code>positionId</code>、<code>positionName</code>、<code>salary</code> 列。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_cols_by_name = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, usecols=[<span class="hljs-string">&#x27;positionId&#x27;</span>, <span class="hljs-string">&#x27;positionName&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>usecols</code> 参数同样可以接收一个包含<strong>列名字符串</strong>的列表，这样更直观。</li></ul></li></ul><h3 id="8-读取-Excel-文件｜指定列（匹配）"><a href="#8-读取-Excel-文件｜指定列（匹配）" class="headerlink" title="8. 读取 Excel 文件｜指定列（匹配）"></a><strong>8. 读取 Excel 文件｜指定列（匹配）</strong></h3><ul><li><p><strong>题目</strong>: 给定一个列表 <code>usecols = [&#39;positionId&#39;,&#39;test&#39;,&#39;positionName&#39;, &#39;test1&#39;,&#39;salary&#39;]</code>，如果列表中的列名存在于 <code>某招聘网站数据.csv</code> 中，则读取。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 首先，我们需要知道原始文件有哪些列</span><br>all_cols = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, nrows=<span class="hljs-number">0</span>).columns.tolist()<br><br><span class="hljs-comment"># 然后找出交集</span><br>cols_to_use = [<span class="hljs-string">&#x27;positionId&#x27;</span>, <span class="hljs-string">&#x27;test&#x27;</span>, <span class="hljs-string">&#x27;positionName&#x27;</span>, <span class="hljs-string">&#x27;test1&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>]<br>valid_cols = [col <span class="hljs-keyword">for</span> col <span class="hljs-keyword">in</span> cols_to_use <span class="hljs-keyword">if</span> col <span class="hljs-keyword">in</span> all_cols]<br><br><span class="hljs-comment"># 最后使用有效的列名列表来读取</span><br>df_matched_cols = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, usecols=valid_cols)<br></code></pre></td></tr></table></figure><p>或者， можно利用 <code>usecols</code> 可以接收一个<strong>函数</strong>的特性，一步到位：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">cols_to_use = [<span class="hljs-string">&#x27;positionId&#x27;</span>,<span class="hljs-string">&#x27;test&#x27;</span>,<span class="hljs-string">&#x27;positionName&#x27;</span>, <span class="hljs-string">&#x27;test1&#x27;</span>,<span class="hljs-string">&#x27;salary&#x27;</span>]<br>df_matched_cols_func = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, usecols=<span class="hljs-keyword">lambda</span> c: c <span class="hljs-keyword">in</span> cols_to_use)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>usecols</code> 接收一个 lambda 函数 <code>lambda c: c in cols_to_use</code>，Pandas 会对文件中的每一个列名 <code>c</code> 执行这个函数，如果返回 <code>True</code>（即列名存在于我们的 <code>cols_to_use</code> 列表中），则该列被读取。这种方法更简洁高效。</li></ul></li></ul><h3 id="9-读取-Excel-文件｜指定索引"><a href="#9-读取-Excel-文件｜指定索引" class="headerlink" title="9. 读取 Excel 文件｜指定索引"></a><strong>9. 读取 Excel 文件｜指定索引</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，并在读取时将 <code>positionId</code> 设置为索引列。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_set_index = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, index_col=<span class="hljs-string">&#x27;positionId&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>index_col</code> 参数可以将指定的列直接设置为 DataFrame 的索引。这可以避免多出一列默认的 0, 1, 2… 整数索引。</li></ul></li></ul><h3 id="10-读取-Excel-文件｜指定标题"><a href="#10-读取-Excel-文件｜指定标题" class="headerlink" title="10. 读取 Excel 文件｜指定标题"></a><strong>10. 读取 Excel 文件｜指定标题</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件的 <code>positionId</code>、<code>positionName</code>、<code>salary</code> 列，并将标题设置为 <code>ID</code>、<code>岗位名称</code>、<code>薪资</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python">df_custom_header = pd.read_csv(<br>    <span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>,<br>    usecols=[<span class="hljs-string">&#x27;positionId&#x27;</span>, <span class="hljs-string">&#x27;positionName&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>],  <span class="hljs-comment"># 先选出想要的列</span><br>    names=[<span class="hljs-string">&#x27;ID&#x27;</span>, <span class="hljs-string">&#x27;岗位名称&#x27;</span>, <span class="hljs-string">&#x27;薪资&#x27;</span>],                <span class="hljs-comment"># 然后提供新的名字</span><br>    header=<span class="hljs-number">0</span>                                       <span class="hljs-comment"># 指明原始文件的第一行是标题行，需要被替换</span><br>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>names</code> 参数可以提供一个列名列表，来作为新的标题。</li><li>当使用 <code>names</code> 时，通常需要搭配 <code>header=0</code>，告诉 Pandas 原始文件的第一行是旧的标题，我们不把它当作数据，而是要用 <code>names</code> 里的内容去覆盖它。</li></ul></li></ul><h3 id="11-读取-Excel-文件｜缺失值转换"><a href="#11-读取-Excel-文件｜缺失值转换" class="headerlink" title="11. 读取 Excel 文件｜缺失值转换"></a><strong>11. 读取 Excel 文件｜缺失值转换</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，并不将缺失值标记为 <code>NA</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_no_na_convert = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, keep_default_na=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>默认情况下，Pandas 会将一些特定的字符串（如 <code>NA</code>, <code>NULL</code>, <code>NaN</code>, <code>&#39;&#39;</code> 等）自动识别并转换成标准的缺失值 <code>np.nan</code>。</li><li><code>keep_default_na=False</code> 可以关闭这个默认行为，让所有数据保持其原始的文本状态。</li><li><strong>思考</strong>: 为什么要这样做？有时数据中本身就包含了像 <code>NA</code> 这样的字符串，但它有实际意义（比如”Not Applicable”的缩写），我们不希望它被误判为缺失值。</li></ul></li></ul><h3 id="12-读取-Excel-文件｜缺失值标记"><a href="#12-读取-Excel-文件｜缺失值标记" class="headerlink" title="12. 读取 Excel 文件｜缺失值标记"></a><strong>12. 读取 Excel 文件｜缺失值标记</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，并将 <code>[]</code> 标记为缺失值。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_custom_na = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, na_values=[<span class="hljs-string">&#x27;[]&#x27;</span>])<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>na_values</code> 参数允许你自定义哪些值应该被视作缺失值。你可以提供一个字符串，或者一个包含多个字符串的列表。</li></ul></li></ul><h3 id="13-读取-Excel-文件｜忽略缺失值"><a href="#13-读取-Excel-文件｜忽略缺失值" class="headerlink" title="13. 读取 Excel 文件｜忽略缺失值"></a><strong>13. 读取 Excel 文件｜忽略缺失值</strong></h3><ul><li><p><strong>题目</strong>: 读取当前目录下 <code>某招聘网站数据.csv</code> 文件，但不处理缺失值。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_no_filter = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, na_filter=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>na_filter=False</code> 参数会完全关闭缺失值的检测。Pandas 不会尝试去寻找任何缺失值。</li><li><strong>思考</strong>: 和 <code>keep_default_na=False</code> 有什么不同？<code>na_filter=False</code> 是性能优化的终极手段。如果能确定数据源非常干净，没有任何缺失值，使用它可以<strong>显著提升大文件的读取速度</strong>，因为它跳过了逐个元素判断是否为缺失值的过程。</li></ul></li></ul><h3 id="14-读取-Excel-文件｜指定格式"><a href="#14-读取-Excel-文件｜指定格式" class="headerlink" title="14. 读取 Excel 文件｜指定格式"></a><strong>14. 读取 Excel 文件｜指定格式</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，并将 <code>positionId</code>, <code>companyId</code> 设置为字符串格式。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_dtype = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, dtype=&#123;<span class="hljs-string">&#x27;positionId&#x27;</span>: <span class="hljs-built_in">str</span>, <span class="hljs-string">&#x27;companyId&#x27;</span>: <span class="hljs-built_in">str</span>&#125;)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>dtype</code> 参数允许我们在读取时就为特定列指定数据类型。</li><li>为什么要这样做？像 ID、邮政编码这类虽然是数字，但没有计算意义，有时还可能以 0 开头，如果 Pandas 自动识别为数字类型，可能会导致精度丢失或前面的 0 消失。提前指定为 <code>str</code> (字符串) 可以避免这些问题。</li></ul></li></ul><h3 id="15-读取-Excel-文件｜指定格式（时间）"><a href="#15-读取-Excel-文件｜指定格式（时间）" class="headerlink" title="15. 读取 Excel 文件｜指定格式（时间）"></a><strong>15. 读取 Excel 文件｜指定格式（时间）</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，并将 <code>createTime</code> 列解析为时间格式。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_parse_dates = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, parse_dates=[<span class="hljs-string">&#x27;createTime&#x27;</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>parse_dates</code> 参数非常方便，它接收一个列名列表，Pandas 会尝试将这些列中的内容自动解析成标准的日期时间格式 (<code>datetime</code>)。这比读取后再用 <code>pd.to_datetime()</code> 函数转换要高效。</li></ul></li></ul><h3 id="16-读取-Excel-文件｜分块读取"><a href="#16-读取-Excel-文件｜分块读取" class="headerlink" title="16. 读取 Excel 文件｜分块读取"></a><strong>16. 读取 Excel 文件｜分块读取</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某招聘网站数据.csv</code> 文件，要求返回一个可迭代对象，每次读取 10 行。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python">chunk_reader = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, chunksize=<span class="hljs-number">10</span>)<br><br><span class="hljs-comment"># 我们可以遍历这个对象</span><br><span class="hljs-keyword">for</span> chunk <span class="hljs-keyword">in</span> chunk_reader:<br>    <span class="hljs-comment"># 对每一块(chunk)进行处理，chunk本身是一个DataFrame</span><br>    <span class="hljs-built_in">print</span>(chunk.shape)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>chunksize</code> 参数用于分块读取。当设置了 <code>chunksize=10</code>，<code>pd.read_csv</code> 不会一次性返回整个 DataFrame，而是返回一个迭代器。</li><li><strong>思考</strong>: 为什么要这样做？当处理一个内存无法容纳的超大文件（例如几十个 GB）时，这是唯一的办法。你可以逐块读取，对每一块进行处理、计算，然后丢弃，从而只占用少量内存。</li></ul></li></ul><h3 id="17-读取-txt-文件｜常规"><a href="#17-读取-txt-文件｜常规" class="headerlink" title="17. 读取 txt 文件｜常规"></a><strong>17. 读取 txt 文件｜常规</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>Titanic.txt</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_txt = pd.read_csv(<span class="hljs-string">&quot;Titanic.txt&quot;</span>) <span class="hljs-comment"># 或者 pd.read_table(&quot;Titanic.txt&quot;)</span><br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_csv()</code> 非常通用，只要文件中的数据有固定的分隔符，它都能处理。对于 txt 文件，如果内容是以逗号分隔的，可以直接用。</li><li><code>pd.read_table()</code> 是一个更通用的函数，它默认使用制表符 <code>\t</code> 作为分隔符，也可以通过 <code>sep</code> 参数指定其他分隔符。</li></ul></li></ul><h3 id="18-读取-txt-文件｜含中文"><a href="#18-读取-txt-文件｜含中文" class="headerlink" title="18. 读取 txt 文件｜含中文"></a><strong>18. 读取 txt 文件｜含中文</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>TOP250.txt</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_chinese_txt = pd.read_csv(<span class="hljs-string">&quot;TOP250.txt&quot;</span>, encoding=<span class="hljs-string">&#x27;utf-8&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>在处理包含中文的文本文件时，经常会遇到编码问题导致 <code>UnicodeDecodeError</code>。这时需要指定正确的<strong>编码格式</strong>。</li><li><code>encoding</code> 参数就是用来解决这个问题的。常见的中文编码有 <code>utf-8</code>、<code>gbk</code>、<code>gb2312</code> 等。<code>utf-8</code> 是最通用的。</li></ul></li></ul><h3 id="19-读取-JSON-文件"><a href="#19-读取-JSON-文件" class="headerlink" title="19. 读取 JSON 文件"></a><strong>19. 读取 JSON 文件</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>某基金数据.json</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_json = pd.read_json(<span class="hljs-string">&quot;某基金数据.json&quot;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_json()</code> 用于读取 JSON 文件。它能自动解析多种格式的 JSON，但最适合的格式是 <code>[&#123;&quot;col1&quot;: &quot;val1&quot;, &quot;col2&quot;: &quot;val2&quot;&#125;, ...]</code> (records orient)。</li></ul></li></ul><h3 id="20-读取-HDF5-文件"><a href="#20-读取-HDF5-文件" class="headerlink" title="20. 读取 HDF5 文件"></a><strong>20. 读取 HDF5 文件</strong></h3><ul><li><p><strong>题目</strong>: 读取 <code>store_tl.h5</code> 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">df_hdf = pd.read_hdf(<span class="hljs-string">&quot;store_tl.h5&quot;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>HDF5 是一种为存储和处理大规模科学数据设计的高性能二进制文件格式。</li><li><code>pd.read_hdf()</code> 用于读取这类文件。读取 HDF5 文件通常比 CSV 快很多，且能保持数据类型不变。使用前可能需要安装 <code>tables</code> 或 <code>h5py</code> 库。</li></ul></li></ul><h3 id="21-从剪贴板读取数据"><a href="#21-从剪贴板读取数据" class="headerlink" title="21. 从剪贴板读取数据"></a><strong>21. 从剪贴板读取数据</strong></h3><ul><li><p><strong>题目</strong>: 打开 <code>Titanic.txt</code>，全选并复制，然后从剪贴板读取。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 操作：先去打开文件，Ctrl+A 全选，Ctrl+C 复制</span><br>df_clipboard = pd.read_clipboard()<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_clipboard()</code> 是一个超级实用的函数！它可以直接将你剪贴板中复制的表格数据（比如来自网页、Excel 或文本文件）转换成 DataFrame。省去了先保存再读取的步骤。</li></ul></li></ul><h3 id="22-从-SQL-读取数据"><a href="#22-从-SQL-读取数据" class="headerlink" title="22. 从 SQL 读取数据"></a><strong>22. 从 SQL 读取数据</strong></h3><ul><li><p><strong>题目</strong>: 将 SQL 查询 <code>SELECT int_column, date_column FROM test_data</code> 转换为 DataFrame。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 题目中给出的准备代码</span><br><span class="hljs-keyword">from</span> sqlite3 <span class="hljs-keyword">import</span> connect<br>conn = connect(<span class="hljs-string">&#x27;:memory:&#x27;</span>) <span class="hljs-comment"># 创建一个内存数据库</span><br>df_source = pd.DataFrame(data=[[<span class="hljs-number">0</span>, <span class="hljs-string">&#x27;10/11/12&#x27;</span>], [<span class="hljs-number">1</span>, <span class="hljs-string">&#x27;12/11/10&#x27;</span>]],<br>                  columns=[<span class="hljs-string">&#x27;int_column&#x27;</span>, <span class="hljs-string">&#x27;date_column&#x27;</span>])<br>df_source.to_sql(<span class="hljs-string">&#x27;test_data&#x27;</span>, conn, index=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 将数据存入数据库</span><br><br><span class="hljs-comment"># 答案代码</span><br>query = <span class="hljs-string">&quot;SELECT int_column, date_column FROM test_data&quot;</span><br>df_sql = pd.read_sql(query, conn)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_sql()</code> 可以直接执行一个 SQL 查询，并将查询结果返回为一个 DataFrame。</li><li>它需要两个主要参数：<code>sql</code> (要执行的查询语句) 和 <code>con</code> (数据库连接对象)。</li></ul></li></ul><h3 id="23-从网页读取数据"><a href="#23-从网页读取数据" class="headerlink" title="23. 从网页读取数据"></a><strong>23. 从网页读取数据</strong></h3><ul><li><p><strong>题目</strong>: 从东京奥运会官网读取奖牌榜数据。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 注意：需要安装 lxml 和 html5lib 库 -&gt; pip install lxml html5lib</span><br>url = <span class="hljs-string">&quot;https://olympics.com/tokyo-2020/olympic-games/zh/results/all-sports/medal-standings.htm&quot;</span><br><span class="hljs-comment"># pd.read_html() 会返回一个包含页面上所有表格的列表</span><br>tables = pd.read_html(url)<br>medal_df = tables[<span class="hljs-number">0</span>] <span class="hljs-comment"># 通常第一个就是我们想要的</span><br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.read_html()</code> 会扫描指定 URL 的 HTML 源码，找出所有的 <code>&lt;table&gt;</code> 标签，并尝试将它们都解析成 DataFrame。</li><li><strong>思考</strong>: 什么类型的在线表格可以直接读取？必须是**静态的 HTML <code>&lt;table&gt;</code>**。由 JavaScript 动态加载出来的数据表格是无法直接读取的。</li></ul></li></ul><h3 id="24-循环读取数据"><a href="#24-循环读取数据" class="headerlink" title="24. 循环读取数据"></a><strong>24. 循环读取数据</strong></h3><ul><li><p><strong>题目</strong>: 一次性循环读取 <code>demodata</code> 文件夹下的所有 Excel 文件。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> os<br><br>path = <span class="hljs-string">&#x27;./demodata/&#x27;</span><br>all_files = os.listdir(path)<br>df_list = []<br><br><span class="hljs-keyword">for</span> file <span class="hljs-keyword">in</span> all_files:<br>    <span class="hljs-comment"># 确保只读取 .xlsx 或 .csv 文件</span><br>    <span class="hljs-keyword">if</span> file.endswith((<span class="hljs-string">&#x27;.xlsx&#x27;</span>, <span class="hljs-string">&#x27;.csv&#x27;</span>)):<br>        file_path = os.path.join(path, file)<br>        df = pd.read_excel(file_path) <span class="hljs-comment"># 或 pd.read_csv()</span><br>        df_list.append(df)<br><br><span class="hljs-comment"># 如果需要将所有数据合并成一个 DataFrame</span><br>combined_df = pd.concat(df_list, ignore_index=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li>首先使用 Python 的 <code>os</code> 模块来获取目标文件夹内所有的文件名。</li><li>然后遍历这个文件列表，使用 <code>os.path.join()</code> 构建每个文件的完整路径。</li><li>逐个读取文件，并将得到的 DataFrame 添加到一个列表中。</li><li>最后，使用 <code>pd.concat()</code> 可以将列表中的所有 DataFrame 合并成一个大的 DataFrame。</li></ul></li></ul><hr><h2 id="1-2-数据创建"><a href="#1-2-数据创建" class="headerlink" title="1-2 数据创建"></a><strong>1-2 数据创建</strong></h2><p>这部分练习如何从 Python 的基础数据结构（如列表、字典）来手动创建 DataFrame。</p><h3 id="25-从列表创建"><a href="#25-从列表创建" class="headerlink" title="25. 从列表创建"></a><strong>25. 从列表创建</strong></h3><ul><li><p><strong>题目</strong>: 将 <code>l = [1,2,3,4,5]</code> 转换为 DataFrame，并指定列名为 <code>&quot;早起Python&quot;</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">l = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>]<br>df_from_list = pd.DataFrame(l, columns=[<span class="hljs-string">&quot;早起Python&quot;</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.DataFrame()</code> 是创建数据框的核心函数。直接传入一个简单列表，会生成一个只有一列的 DataFrame。</li><li><code>columns</code> 参数用于指定列名。</li></ul></li></ul><h3 id="26-从列表创建｜嵌套列表"><a href="#26-从列表创建｜嵌套列表" class="headerlink" title="26. 从列表创建｜嵌套列表"></a><strong>26. 从列表创建｜嵌套列表</strong></h3><ul><li><p><strong>题目</strong>: 将 <code>l = [[1,2,3],[4,5,6]]</code> 转换为 DataFrame，并指定行索引为 <code>&quot;公众号&quot;</code>, <code>&quot;早起Python&quot;</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">l = [[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>], [<span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>]]<br>df_from_nested_list = pd.DataFrame(l, index=[<span class="hljs-string">&quot;公众号&quot;</span>, <span class="hljs-string">&quot;早起Python&quot;</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>当传入一个嵌套列表时，每个内部列表会被当作一行。</li><li><code>index</code> 参数用于指定行索引（标签）。</li></ul></li></ul><h3 id="27-从字典创建"><a href="#27-从字典创建" class="headerlink" title="27. 从字典创建"></a><strong>27. 从字典创建</strong></h3><ul><li><p><strong>题目</strong>: 将字典 <code>d</code> 转换为 DataFrame。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python">d = &#123;<br>    <span class="hljs-string">&quot;one&quot;</span>: pd.Series([<span class="hljs-number">1.0</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">3.0</span>], index=[<span class="hljs-string">&quot;a&quot;</span>, <span class="hljs-string">&quot;b&quot;</span>, <span class="hljs-string">&quot;c&quot;</span>]),<br>    <span class="hljs-string">&quot;two&quot;</span>: pd.Series([<span class="hljs-number">1.0</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">3.0</span>, <span class="hljs-number">4.0</span>], index=[<span class="hljs-string">&quot;a&quot;</span>, <span class="hljs-string">&quot;b&quot;</span>, <span class="hljs-string">&quot;c&quot;</span>, <span class="hljs-string">&quot;d&quot;</span>])<br>&#125;<br>df_from_dict = pd.DataFrame(d)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>当用字典创建 DataFrame 时，字典的<strong>键 (key)</strong> 会成为<strong>列名</strong>，<strong>值 (value)</strong> 成为该列的数据。</li><li>Pandas 会自动根据所有 Series 的索引进行<strong>对齐</strong>。比如 <code>one</code> 这个 Series 在索引 <code>d</code> 上没有值，所以在生成的 DataFrame 中，<code>one</code> 列 <code>d</code> 行的位置会是 <code>NaN</code> (缺失值)。</li></ul></li></ul><h3 id="28-从字典创建｜指定索引"><a href="#28-从字典创建｜指定索引" class="headerlink" title="28. 从字典创建｜指定索引"></a><strong>28. 从字典创建｜指定索引</strong></h3><ul><li><p><strong>题目</strong>: 将字典 <code>d</code> 转换为 DataFrame，并指定索引顺序为 <code>d</code>、<code>b</code>、<code>a</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 沿用上一题的字典 d</span><br>df_from_dict_reindex = pd.DataFrame(d, index=[<span class="hljs-string">&#x27;d&#x27;</span>, <span class="hljs-string">&#x27;b&#x27;</span>, <span class="hljs-string">&#x27;a&#x27;</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>index</code> 参数可以用来从原始数据中挑选出我们想要的行，并按照我们指定的顺序排列。</li></ul></li></ul><h3 id="29-从字典创建｜指定列名"><a href="#29-从字典创建｜指定列名" class="headerlink" title="29. 从字典创建｜指定列名"></a><strong>29. 从字典创建｜指定列名</strong></h3><ul><li><p><strong>题目</strong>: 将字典 <code>d</code> 转换为 DataFrame，索引为 <code>d</code>、<code>b</code>、<code>a</code>，列名为 <code>&quot;two&quot;</code>, <code>&quot;three&quot;</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 沿用上一题的字典 d</span><br>df_from_dict_recol = pd.DataFrame(d, index=[<span class="hljs-string">&#x27;d&#x27;</span>, <span class="hljs-string">&#x27;b&#x27;</span>, <span class="hljs-string">&#x27;a&#x27;</span>], columns=[<span class="hljs-string">&#x27;two&#x27;</span>, <span class="hljs-string">&#x27;three&#x27;</span>])<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>columns</code> 参数同样可以起到筛选和重排的作用。这里我们只选择了 <code>two</code> 列，并尝试选择一个不存在的 <code>three</code> 列。</li><li>最终生成的 DataFrame 会包含 <code>two</code> 列（根据指定的 <code>index</code>），和一列全为 <code>NaN</code> 的 <code>three</code> 列。</li></ul></li></ul><h3 id="30-从字典创建｜字典列表"><a href="#30-从字典创建｜字典列表" class="headerlink" title="30. 从字典创建｜字典列表"></a><strong>30. 从字典创建｜字典列表</strong></h3><ul><li><p><strong>题目</strong>: 将 <code>d = [&#123;&quot;a&quot;: 1, &quot;b&quot;: 2&#125;, &#123;&quot;a&quot;: 5, &quot;b&quot;: 10, &quot;c&quot;: 20&#125;]</code> 转换为 DataFrame。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">d = [&#123;<span class="hljs-string">&quot;a&quot;</span>: <span class="hljs-number">1</span>, <span class="hljs-string">&quot;b&quot;</span>: <span class="hljs-number">2</span>&#125;, &#123;<span class="hljs-string">&quot;a&quot;</span>: <span class="hljs-number">5</span>, <span class="hljs-string">&quot;b&quot;</span>: <span class="hljs-number">10</span>, <span class="hljs-string">&quot;c&quot;</span>: <span class="hljs-number">20</span>&#125;]<br>df_from_list_dict = pd.DataFrame(d)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>当传入一个由字典组成的列表时，每个字典会被当作一行。字典的键会自动成为列名。</li><li>Pandas 同样会自动对齐。第一个字典没有键 <code>c</code>，所以在生成的第一行中，<code>c</code> 列的值为 <code>NaN</code>。</li><li><strong>思考</strong>: 如何指定行列索引？可以使用 <code>pd.DataFrame(d, index=[&#39;row1&#39;, &#39;row2&#39;])</code> 来指定行索引。列索引（列名）是由字典的键自动推断的，如果想重排或筛选，可以在创建后再进行 <code>df = df[[&#39;a&#39;, &#39;c&#39;, &#39;b&#39;]]</code> 这样的操作。</li></ul></li></ul><h3 id="31-从集合创建"><a href="#31-从集合创建" class="headerlink" title="31. 从集合创建"></a><strong>31. 从集合创建</strong></h3><ul><li><p><strong>题目</strong>: 将元组 <code>t =((1,0,0,0,),(2,3,0,0,),(4,5,6,0,),(7,8,9,10,))</code> 转换为 DataFrame 且行列索引均为 <code>1,2,3,4</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">t = ((<span class="hljs-number">1</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>,), (<span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>,), (<span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>, <span class="hljs-number">0</span>,), (<span class="hljs-number">7</span>, <span class="hljs-number">8</span>, <span class="hljs-number">9</span>, <span class="hljs-number">10</span>,))<br>df_from_tuple = pd.DataFrame(t, index=[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>], columns=[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>])<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>pd.DataFrame()</code> 函数同样可以很好地处理元组或嵌套元组，其行为与处理列表时完全一样。</li></ul></li></ul><hr><h2 id="1-3-数据存储"><a href="#1-3-数据存储" class="headerlink" title="1-3 数据存储"></a><strong>1-3 数据存储</strong></h2><p>这部分练习如何将内存中的 DataFrame 保存到不同格式的文件中。</p><h3 id="32-保存为-CSV"><a href="#32-保存为-CSV" class="headerlink" title="32. 保存为 CSV"></a><strong>32. 保存为 CSV</strong></h3><ul><li><p><strong>题目</strong>: 将第三题读取到的数据保存为 <code>csv</code> 格式。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 先获取第三题的数据</span><br>data = pd.read_csv(<span class="hljs-string">&quot;某招聘网站数据.csv&quot;</span>, nrows = <span class="hljs-number">20</span>)<br><span class="hljs-comment"># 保存</span><br>data.to_csv(<span class="hljs-string">&quot;output_data.csv&quot;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_csv()</code> 是 DataFrame 对象的一个方法，用于将其内容写入到一个 CSV 文件中。</li></ul></li></ul><h3 id="33-保存为-CSV｜指定列"><a href="#33-保存为-CSV｜指定列" class="headerlink" title="33. 保存为 CSV｜指定列"></a><strong>33. 保存为 CSV｜指定列</strong></h3><ul><li><p><strong>题目</strong>: 只保留 <code>positionName</code>、<code>salary</code> 两列进行保存。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">data.to_csv(<span class="hljs-string">&quot;output_columns.csv&quot;</span>, columns=[<span class="hljs-string">&#x27;positionName&#x27;</span>, <span class="hljs-string">&#x27;salary&#x27;</span>])<br></code></pre></td></tr></table></figure><ul><li><strong>解释</strong>:</li><li><code>columns</code> 参数可以指定只将哪些列保存到文件中。</li></ul></li></ul><h3 id="34-保存为-CSV｜取消索引"><a href="#34-保存为-CSV｜取消索引" class="headerlink" title="34. 保存为 CSV｜取消索引"></a><strong>34. 保存为 CSV｜取消索引</strong></h3><ul><li><p><strong>题目</strong>: 保存时取消每一行的索引。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">data.to_csv(<span class="hljs-string">&quot;output_no_index.csv&quot;</span>, index=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li>默认情况下，<code>to_csv()</code> 会将 DataFrame 的索引也作为一列写入文件。这通常不是我们想要的。</li><li><code>index=False</code> 是一个非常常用的参数，用于告诉 Pandas 不要保存索引。</li></ul></li></ul><h3 id="35-保存为-CSV｜标记缺失值"><a href="#35-保存为-CSV｜标记缺失值" class="headerlink" title="35. 保存为 CSV｜标记缺失值"></a><strong>35. 保存为 CSV｜标记缺失值</strong></h3><ul><li><p><strong>题目</strong>: 在上一题的基础上，将缺失值标记为 <code>&#39;数据缺失&#39;</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">data.to_csv(<span class="hljs-string">&quot;output_na_rep.csv&quot;</span>, index=<span class="hljs-literal">False</span>, na_rep=<span class="hljs-string">&#x27;数据缺失&#x27;</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>na_rep</code> (NA representation) 参数用于指定在保存时，用哪个字符串来表示缺失值 <code>NaN</code>。</li></ul></li></ul><h3 id="36-保存为CSV｜压缩"><a href="#36-保存为CSV｜压缩" class="headerlink" title="36. 保存为CSV｜压缩"></a><strong>36. 保存为CSV｜压缩</strong></h3><ul><li><p><strong>题目</strong>: 将数据保存至 <code>zip</code> 文件，解压后出现 <code>out.csv</code>。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">compression_opts = <span class="hljs-built_in">dict</span>(method=<span class="hljs-string">&#x27;zip&#x27;</span>, archive_name=<span class="hljs-string">&#x27;out.csv&#x27;</span>)<br>data.to_csv(<span class="hljs-string">&#x27;output_compressed.zip&#x27;</span>, index=<span class="hljs-literal">False</span>, compression=compression_opts)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_csv</code> 的 <code>compression</code> 参数能够直接将输出内容压缩。</li><li><code>method=&#39;zip&#39;</code> 指定压缩格式为 zip。</li><li><code>archive_name=&#39;out.csv&#39;</code> 指定了在压缩包内部的文件名。</li></ul></li></ul><h3 id="37-保存为-Excel"><a href="#37-保存为-Excel" class="headerlink" title="37. 保存为 Excel"></a><strong>37. 保存为 Excel</strong></h3><ul><li><p><strong>题目</strong>: 将数据保存为 <code>xlsx</code> 格式。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 注意: 需要安装 openpyxl 库 -&gt; pip install openpyxl</span><br>data.to_excel(<span class="hljs-string">&quot;output_data.xlsx&quot;</span>, index=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_excel()</code> 方法用于将 DataFrame 保存为 Excel 文件。同样建议使用 <code>index=False</code>。</li></ul></li></ul><h3 id="38-保存为-JSON"><a href="#38-保存为-JSON" class="headerlink" title="38. 保存为 JSON"></a><strong>38. 保存为 JSON</strong></h3><ul><li><p><strong>题目</strong>: 将数据保存为 <code>json</code> 格式。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">data.to_json(<span class="hljs-string">&quot;output_data.json&quot;</span>, orient=<span class="hljs-string">&#x27;records&#x27;</span>, force_ascii=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_json()</code> 用于保存为 JSON 格式。</li><li><code>orient=&#39;records&#39;</code> 会生成 <code>[&#123;&quot;col1&quot;: val1&#125;, &#123;&quot;col2&quot;: val2&#125;]</code> 这种常见的格式。</li><li><code>force_ascii=False</code> 确保文件中的中文能正常显示而不是被转义成 ASCII 码。</li></ul></li></ul><h3 id="39-保存为-Markdown"><a href="#39-保存为-Markdown" class="headerlink" title="39. 保存为 Markdown"></a><strong>39. 保存为 Markdown</strong></h3><ul><li><p><strong>题目</strong>: 将数据转换为 <code>markdown</code> 形式的表格。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">markdown_table = data.to_markdown()<br><span class="hljs-built_in">print</span>(markdown_table)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_markdown()</code> 可以生成 Markdown 语法的表格字符串，非常方便直接粘贴到 <code>.md</code> 文件或支持 Markdown 的平台中。</li></ul></li></ul><h3 id="40-保存为-Html"><a href="#40-保存为-Html" class="headerlink" title="40. 保存为 Html"></a><strong>40. 保存为 Html</strong></h3><ul><li><p><strong>题目</strong>: 保存为 <code>html</code> 并进行格式设置。</p></li><li><p><strong>答案</strong>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python">data.to_html(<br>    <span class="hljs-string">&quot;output_data.html&quot;</span>,<br>    index=<span class="hljs-literal">False</span>,<br>    justify=<span class="hljs-string">&#x27;center&#x27;</span>,<br>    col_space=<span class="hljs-number">100</span><br>)<br></code></pre></td></tr></table></figure></li><li><p><strong>解释</strong>:</p><ul><li><code>to_html()</code> 可以将 DataFrame 转换成一个 HTML 表格文件。</li><li><code>justify=&#39;center&#39;</code> 用于设置标题居中对齐。</li><li><code>col_space=100</code> 用于设置列的最小宽度。</li></ul></li></ul>]]></content>
      
      
      <categories>
          
          <category> pandas </category>
          
      </categories>
      
      
        <tags>
            
            <tag> pandas </tag>
            
            <tag> 数据处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>这是一篇测试的博文</title>
      <link href="/2024/%E8%BF%99%E6%98%AF%E4%B8%80%E7%AF%87%E6%96%B0%E7%9A%84%E5%8D%9A%E6%96%87/"/>
      <url>/2024/%E8%BF%99%E6%98%AF%E4%B8%80%E7%AF%87%E6%96%B0%E7%9A%84%E5%8D%9A%E6%96%87/</url>
      
        <content type="html"><![CDATA[<div class="video-container">【AMD发布锐龙9 9950X3D处理器】 https://www.bilibili.com/video/BV1P9cVeMEtk/?share_source=copy_web&vd_source=de1b19532eef016187d22c9630496426</div><style>.video-container {    position: relative;    width: 100%;    padding-top: 56.25%; /* 16:9 aspect ratio (height/width = 9/16 * 100%) */}.video-container iframe {    position: absolute;    top: 0;    left: 0;    width: 100%;    height: 100%;}</style>]]></content>
      
      
      <categories>
          
          <category> 这是一个分类 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 这是一个标签 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo 命令</title>
      <link href="/2024/Hexo%20%E5%91%BD%E4%BB%A4/"/>
      <url>/2024/Hexo%20%E5%91%BD%E4%BB%A4/</url>
      
        <content type="html"><![CDATA[<p>Hexo 框架可以帮助我们快速创建一个属于自己的博客网站，熟悉 Hexo 框架提供的命令有利于我们管理博客</p><h2 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h2><h3 id="hexo-init"><a href="#hexo-init" class="headerlink" title="hexo init"></a><code>hexo init</code></h3><p><code>hexo init</code> 命令用于初始化本地文件夹为网站的根目录。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo init blog-demo<br></code></pre></td></tr></table></figure><h3 id="hexo-new"><a href="#hexo-new" class="headerlink" title="hexo new"></a><code>hexo new</code></h3><p><code>hexo new</code> 命令用于创建一个新的文章，一般可以简写为 <code>hexo n</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo new [layout] &lt;title&gt;<br></code></pre></td></tr></table></figure><p>layout 可选参数，用以指定文章类型，若无指定则默认由配置文件中的 default_layout 选项决定</p><p>title 必填参数，用以指定文章标题，如果参数值中含有空格，则需要使用双引号包围</p><h3 id="hexo-generate"><a href="#hexo-generate" class="headerlink" title="hexo generate"></a><code>hexo generate</code></h3><p><code>hexo generate</code> 命令用于生成静态文件，一般可以简写为 <code>hexo g</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo generate<br></code></pre></td></tr></table></figure><p>-d 选项，指定生成后部署，与 hexo d -g 等价<br>生成静态文件后，会在根目录下生成 public 文件夹，里面包含所有生成的静态文件</p><h3 id="hexo-server"><a href="#hexo-server" class="headerlink" title="hexo server"></a><code>hexo server</code></h3><p><code>hexo server</code> 命令用于启动一个本地服务器，一般可以简写为 <code>hexo s</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo server<br></code></pre></td></tr></table></figure><p>-p 选项，指定端口号<br>-i 选项，指定 IP 地址<br>-s 选项，静态模式 ，仅提供 public 文件夹中的文件并禁用文件监视</p><p>说明 ：运行服务器前需要安装 hexo-server 插件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-server --save<br></code></pre></td></tr></table></figure><h3 id="hexo-deploy"><a href="#hexo-deploy" class="headerlink" title="hexo deploy"></a><code>hexo deploy</code></h3><p><code>hexo deploy</code> 命令用于部署到远程服务器，一般可以简写为 <code>hexo d</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo deploy<br></code></pre></td></tr></table></figure><p>说明 ：部署前需要修改 _config.yml 配置文件</p><h3 id="hexo-clean"><a href="#hexo-clean" class="headerlink" title="hexo clean"></a><code>hexo clean</code></h3><p><code>hexo clean</code> 命令用于清理缓存文件，一般可以简写为 <code>hexo c</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean<br></code></pre></td></tr></table></figure><h2 id="本地启动服务"><a href="#本地启动服务" class="headerlink" title="本地启动服务"></a>本地启动服务</h2><p><strong>Windows:</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo cl; hexo s<br></code></pre></td></tr></table></figure><p><strong>Linux:</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo cl &amp;&amp; hexo s<br></code></pre></td></tr></table></figure><h2 id="一键部署命令"><a href="#一键部署命令" class="headerlink" title="一键部署命令"></a>一键部署命令</h2><p><strong>Windows:</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo cl; hexo g; hexo d<br></code></pre></td></tr></table></figure><p><strong>Linux:</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo cl &amp;&amp; hexo g &amp;&amp; hexo d<br></code></pre></td></tr></table></figure><p>hexo clean：删除之前生成的文件，可以用hexo cl缩写。<br>hexo generate：生成静态文章，可以用hexo g缩写<br>hexo deploy：部署文章，可以用hexo d缩写</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://hexo.io/docs/">Hexo 官方文档</a></li><li><a href="https://vercel.blog.cmliussss.com/p/HexoBlogNo1/">CM</a></li><li><a href="https://vercel.blog.cmliussss.com/p/HexoBlogNo2/">CM2</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Hexo </tag>
            
            <tag> 命令 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
